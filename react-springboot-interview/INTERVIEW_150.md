# 150+ React Interview Questions (backend→frontend)

Each item: Question · 30s · Code · Diagram · Why · Mistake · Follow-up · Senior.
Code pointers: `react-springboot-interview/frontend` and `backend`.

# React Fundamentals

## Q1. What is a React element vs component?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: What is a React element vs component?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `What is a React element vs component?`, start from user-visible behavior and work inward to API/DB.

## Q2. Fragments purpose?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Fragments purpose?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Fragments purpose?`, start from user-visible behavior and work inward to API/DB.

## Q3. Children prop?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Children prop?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Children prop?`, start from user-visible behavior and work inward to API/DB.

## Q4. Composition vs inheritance?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Composition vs inheritance?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Composition vs inheritance?`, start from user-visible behavior and work inward to API/DB.

## Q5. Conditional rendering patterns?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Conditional rendering patterns?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Conditional rendering patterns?`, start from user-visible behavior and work inward to API/DB.

## Q6. Lists without keys risk?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Lists without keys risk?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Lists without keys risk?`, start from user-visible behavior and work inward to API/DB.

## Q7. Events in React synthetic?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Events in React synthetic?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Events in React synthetic?`, start from user-visible behavior and work inward to API/DB.

## Q8. Default props today?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Default props today?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Default props today?`, start from user-visible behavior and work inward to API/DB.

## Q9. Pure components meaning?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Pure components meaning?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Pure components meaning?`, start from user-visible behavior and work inward to API/DB.

## Q10. Why immutability?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Why immutability?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Why immutability?`, start from user-visible behavior and work inward to API/DB.

## Q11. Lifting state up?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Lifting state up?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Lifting state up?`, start from user-visible behavior and work inward to API/DB.

## Q12. Derived state anti-pattern?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Derived state anti-pattern?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Derived state anti-pattern?`, start from user-visible behavior and work inward to API/DB.

## Q13. Controlled checkbox?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Controlled checkbox?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Controlled checkbox?`, start from user-visible behavior and work inward to API/DB.

## Q14. Uncontrolled file input?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Uncontrolled file input?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Uncontrolled file input?`, start from user-visible behavior and work inward to API/DB.

## Q15. Portals use case?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Portals use case?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Portals use case?`, start from user-visible behavior and work inward to API/DB.

## Q16. What is createRoot?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: What is createRoot?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `What is createRoot?`, start from user-visible behavior and work inward to API/DB.

# Hooks

## Q17. Can hooks be async functions?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Can hooks be async functions?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Can hooks be async functions?`, start from user-visible behavior and work inward to API/DB.

## Q18. useLayoutEffect vs useEffect?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: useLayoutEffect vs useEffect?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `useLayoutEffect vs useEffect?`, start from user-visible behavior and work inward to API/DB.

## Q19. useId purpose?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: useId purpose?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `useId purpose?`, start from user-visible behavior and work inward to API/DB.

## Q20. useSyncExternalStore?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: useSyncExternalStore?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `useSyncExternalStore?`, start from user-visible behavior and work inward to API/DB.

## Q21. Custom hook naming?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Custom hook naming?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Custom hook naming?`, start from user-visible behavior and work inward to API/DB.

## Q22. Dependency array pitfalls?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Dependency array pitfalls?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Dependency array pitfalls?`, start from user-visible behavior and work inward to API/DB.

## Q23. Exhaustive deps eslint?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Exhaustive deps eslint?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Exhaustive deps eslint?`, start from user-visible behavior and work inward to API/DB.

## Q24. Effect vs event handler?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Effect vs event handler?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Effect vs event handler?`, start from user-visible behavior and work inward to API/DB.

## Q25. Subscribing to window resize?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Subscribing to window resize?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Subscribing to window resize?`, start from user-visible behavior and work inward to API/DB.

## Q26. Cleaning timers?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Cleaning timers?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Cleaning timers?`, start from user-visible behavior and work inward to API/DB.

## Q27. useReducer init function?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: useReducer init function?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `useReducer init function?`, start from user-visible behavior and work inward to API/DB.

## Q28. Bail out of useState?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Bail out of useState?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Bail out of useState?`, start from user-visible behavior and work inward to API/DB.

## Q29. useImperativeHandle legacy?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: useImperativeHandle legacy?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `useImperativeHandle legacy?`, start from user-visible behavior and work inward to API/DB.

## Q30. useDebugValue?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: useDebugValue?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `useDebugValue?`, start from user-visible behavior and work inward to API/DB.

## Q31. Rules violation example?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Rules violation example?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Rules violation example?`, start from user-visible behavior and work inward to API/DB.

## Q32. Sharing state between hooks?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Sharing state between hooks?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Sharing state between hooks?`, start from user-visible behavior and work inward to API/DB.

# Rendering

## Q33. Render phase purity?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Render phase purity?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Render phase purity?`, start from user-visible behavior and work inward to API/DB.

## Q34. Commit phase side effects?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Commit phase side effects?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Commit phase side effects?`, start from user-visible behavior and work inward to API/DB.

## Q35. What is tearing?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: What is tearing?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `What is tearing?`, start from user-visible behavior and work inward to API/DB.

## Q36. Concurrent rendering benefit?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Concurrent rendering benefit?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Concurrent rendering benefit?`, start from user-visible behavior and work inward to API/DB.

## Q37. startTransition vs debounce?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: startTransition vs debounce?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `startTransition vs debounce?`, start from user-visible behavior and work inward to API/DB.

## Q38. Blocking render symptoms?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Blocking render symptoms?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Blocking render symptoms?`, start from user-visible behavior and work inward to API/DB.

## Q39. Key changing remounts?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Key changing remounts?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Key changing remounts?`, start from user-visible behavior and work inward to API/DB.

## Q40. FlushSync when?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: FlushSync when?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `FlushSync when?`, start from user-visible behavior and work inward to API/DB.

## Q41. Batching across promises?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Batching across promises?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Batching across promises?`, start from user-visible behavior and work inward to API/DB.

## Q42. Why render runs twice in StrictMode?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Why render runs twice in StrictMode?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Why render runs twice in StrictMode?`, start from user-visible behavior and work inward to API/DB.

## Q43. Priority of updates?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Priority of updates?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Priority of updates?`, start from user-visible behavior and work inward to API/DB.

## Q44. Selective hydration concept?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Selective hydration concept?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Selective hydration concept?`, start from user-visible behavior and work inward to API/DB.

# Reconciliation

## Q45. How does React diff lists?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: How does React diff lists?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `How does React diff lists?`, start from user-visible behavior and work inward to API/DB.

## Q46. Sibling keys uniqueness?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Sibling keys uniqueness?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Sibling keys uniqueness?`, start from user-visible behavior and work inward to API/DB.

## Q47. Reordering with keys?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Reordering with keys?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Reordering with keys?`, start from user-visible behavior and work inward to API/DB.

## Q48. Type change remount?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Type change remount?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Type change remount?`, start from user-visible behavior and work inward to API/DB.

## Q49. Fiber node role?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Fiber node role?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Fiber node role?`, start from user-visible behavior and work inward to API/DB.

## Q50. Single child reconcile?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Single child reconcile?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Single child reconcile?`, start from user-visible behavior and work inward to API/DB.

## Q51. Fragment reconcile?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Fragment reconcile?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Fragment reconcile?`, start from user-visible behavior and work inward to API/DB.

## Q52. Memoized element reuse?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Memoized element reuse?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Memoized element reuse?`, start from user-visible behavior and work inward to API/DB.

# Performance

## Q53. When is useMemo harmful?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: When is useMemo harmful?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `When is useMemo harmful?`, start from user-visible behavior and work inward to API/DB.

## Q54. Inline functions and memo?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Inline functions and memo?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Inline functions and memo?`, start from user-visible behavior and work inward to API/DB.

## Q55. Virtualization libraries?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Virtualization libraries?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Virtualization libraries?`, start from user-visible behavior and work inward to API/DB.

## Q56. Windowing vs pagination?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Windowing vs pagination?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Windowing vs pagination?`, start from user-visible behavior and work inward to API/DB.

## Q57. Bundle analyzer use?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Bundle analyzer use?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Bundle analyzer use?`, start from user-visible behavior and work inward to API/DB.

## Q58. Tree shaking?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Tree shaking?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Tree shaking?`, start from user-visible behavior and work inward to API/DB.

## Q59. Avoiding giant context value?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Avoiding giant context value?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Avoiding giant context value?`, start from user-visible behavior and work inward to API/DB.

## Q60. Selector equality functions?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Selector equality functions?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Selector equality functions?`, start from user-visible behavior and work inward to API/DB.

## Q61. INP optimization?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: INP optimization?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `INP optimization?`, start from user-visible behavior and work inward to API/DB.

## Q62. Image lazy loading?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Image lazy loading?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Image lazy loading?`, start from user-visible behavior and work inward to API/DB.

## Q63. Prefetch on hover?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Prefetch on hover?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Prefetch on hover?`, start from user-visible behavior and work inward to API/DB.

## Q64. React Compiler impact?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: React Compiler impact?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `React Compiler impact?`, start from user-visible behavior and work inward to API/DB.

# State Management

## Q65. URL as source of truth?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: URL as source of truth?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `URL as source of truth?`, start from user-visible behavior and work inward to API/DB.

## Q66. Zustand vs Redux?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Zustand vs Redux?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Zustand vs Redux?`, start from user-visible behavior and work inward to API/DB.

## Q67. Normalizing entities?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Normalizing entities?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Normalizing entities?`, start from user-visible behavior and work inward to API/DB.

## Q68. Optimistic rollback pattern?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Optimistic rollback pattern?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Optimistic rollback pattern?`, start from user-visible behavior and work inward to API/DB.

## Q69. Form state location?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Form state location?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Form state location?`, start from user-visible behavior and work inward to API/DB.

## Q70. Global modal state?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Global modal state?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Global modal state?`, start from user-visible behavior and work inward to API/DB.

## Q71. Server cache invalidation?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Server cache invalidation?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Server cache invalidation?`, start from user-visible behavior and work inward to API/DB.

## Q72. Race with two filters?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Race with two filters?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Race with two filters?`, start from user-visible behavior and work inward to API/DB.

## Q73. Derived totals from list?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Derived totals from list?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Derived totals from list?`, start from user-visible behavior and work inward to API/DB.

## Q74. Persist state?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Persist state?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Persist state?`, start from user-visible behavior and work inward to API/DB.

## Q75. State machines?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: State machines?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `State machines?`, start from user-visible behavior and work inward to API/DB.

## Q76. Why not one mega context?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Why not one mega context?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Why not one mega context?`, start from user-visible behavior and work inward to API/DB.

# Routing

## Q77. Nested routes?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Nested routes?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Nested routes?`, start from user-visible behavior and work inward to API/DB.

## Q78. Loader concepts (framework)?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Loader concepts (framework)?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Loader concepts (framework)?`, start from user-visible behavior and work inward to API/DB.

## Q79. Protected route redirect?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Protected route redirect?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Protected route redirect?`, start from user-visible behavior and work inward to API/DB.

## Q80. Lazy route error?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Lazy route error?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Lazy route error?`, start from user-visible behavior and work inward to API/DB.

## Q81. Search params sync?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Search params sync?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Search params sync?`, start from user-visible behavior and work inward to API/DB.

## Q82. 404 strategy?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: 404 strategy?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `404 strategy?`, start from user-visible behavior and work inward to API/DB.

## Q83. Programmatic navigate?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Programmatic navigate?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Programmatic navigate?`, start from user-visible behavior and work inward to API/DB.

## Q84. Relative links?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Relative links?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Relative links?`, start from user-visible behavior and work inward to API/DB.

# Forms

## Q85. React Hook Form benefit?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: React Hook Form benefit?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `React Hook Form benefit?`, start from user-visible behavior and work inward to API/DB.

## Q86. Native form Action React19?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Native form Action React19?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Native form Action React19?`, start from user-visible behavior and work inward to API/DB.

## Q87. Validation UX?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Validation UX?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Validation UX?`, start from user-visible behavior and work inward to API/DB.

## Q88. Disable double submit?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Disable double submit?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Disable double submit?`, start from user-visible behavior and work inward to API/DB.

## Q89. File upload progress?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: File upload progress?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `File upload progress?`, start from user-visible behavior and work inward to API/DB.

## Q90. Multi-step forms state?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Multi-step forms state?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Multi-step forms state?`, start from user-visible behavior and work inward to API/DB.

## Q91. Schema validation (zod)?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Schema validation (zod)?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Schema validation (zod)?`, start from user-visible behavior and work inward to API/DB.

## Q92. Accessibility labels?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Accessibility labels?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Accessibility labels?`, start from user-visible behavior and work inward to API/DB.

# API Integration

## Q93. Central apiClient why?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Central apiClient why?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Central apiClient why?`, start from user-visible behavior and work inward to API/DB.

## Q94. Timeouts?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Timeouts?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Timeouts?`, start from user-visible behavior and work inward to API/DB.

## Q95. Idempotency-Key?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Idempotency-Key?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Idempotency-Key?`, start from user-visible behavior and work inward to API/DB.

## Q96. Partial failure UI?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Partial failure UI?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Partial failure UI?`, start from user-visible behavior and work inward to API/DB.

## Q97. Upload vs JSON?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Upload vs JSON?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Upload vs JSON?`, start from user-visible behavior and work inward to API/DB.

## Q98. Pagination cursor?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Pagination cursor?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Pagination cursor?`, start from user-visible behavior and work inward to API/DB.

## Q99. ETags/conditional GET?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: ETags/conditional GET?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `ETags/conditional GET?`, start from user-visible behavior and work inward to API/DB.

## Q100. OpenAPI codegen?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: OpenAPI codegen?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `OpenAPI codegen?`, start from user-visible behavior and work inward to API/DB.

# Security

## Q101. CSP basics?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: CSP basics?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `CSP basics?`, start from user-visible behavior and work inward to API/DB.

## Q102. dangerouslySetInnerHTML?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: dangerouslySetInnerHTML?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `dangerouslySetInnerHTML?`, start from user-visible behavior and work inward to API/DB.

## Q103. Token in memory only?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Token in memory only?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Token in memory only?`, start from user-visible behavior and work inward to API/DB.

## Q104. Clickjacking?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Clickjacking?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Clickjacking?`, start from user-visible behavior and work inward to API/DB.

## Q105. Dependency pinning?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Dependency pinning?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Dependency pinning?`, start from user-visible behavior and work inward to API/DB.

## Q106. Secret in Vite env?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Secret in Vite env?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Secret in Vite env?`, start from user-visible behavior and work inward to API/DB.

## Q107. Log redaction?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Log redaction?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Log redaction?`, start from user-visible behavior and work inward to API/DB.

## Q108. Admin UI threats?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Admin UI threats?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Admin UI threats?`, start from user-visible behavior and work inward to API/DB.

# Testing

## Q109. MSW purpose?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: MSW purpose?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `MSW purpose?`, start from user-visible behavior and work inward to API/DB.

## Q110. Testing hooks?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Testing hooks?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Testing hooks?`, start from user-visible behavior and work inward to API/DB.

## Q111. E2E flake causes?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: E2E flake causes?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `E2E flake causes?`, start from user-visible behavior and work inward to API/DB.

## Q112. Contract tests?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Contract tests?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Contract tests?`, start from user-visible behavior and work inward to API/DB.

## Q113. Visual regression?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Visual regression?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Visual regression?`, start from user-visible behavior and work inward to API/DB.

## Q114. A11y tests?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: A11y tests?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `A11y tests?`, start from user-visible behavior and work inward to API/DB.

## Q115. Mock router?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Mock router?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Mock router?`, start from user-visible behavior and work inward to API/DB.

## Q116. Coverage vanity?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Coverage vanity?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Coverage vanity?`, start from user-visible behavior and work inward to API/DB.

# Architecture

## Q117. Feature folders?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Feature folders?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Feature folders?`, start from user-visible behavior and work inward to API/DB.

## Q118. Design system package?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Design system package?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Design system package?`, start from user-visible behavior and work inward to API/DB.

## Q119. Monorepo?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Monorepo?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Monorepo?`, start from user-visible behavior and work inward to API/DB.

## Q120. Module federation?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Module federation?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Module federation?`, start from user-visible behavior and work inward to API/DB.

## Q121. BFF per channel?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: BFF per channel?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `BFF per channel?`, start from user-visible behavior and work inward to API/DB.

## Q122. Error taxonomy?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Error taxonomy?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Error taxonomy?`, start from user-visible behavior and work inward to API/DB.

## Q123. Feature flag cleanup?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Feature flag cleanup?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Feature flag cleanup?`, start from user-visible behavior and work inward to API/DB.

## Q124. Multi-region FE?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Multi-region FE?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Multi-region FE?`, start from user-visible behavior and work inward to API/DB.

# SSR

## Q125. TTFB vs TTI?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: TTFB vs TTI?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `TTFB vs TTI?`, start from user-visible behavior and work inward to API/DB.

## Q126. Streaming SSR?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Streaming SSR?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Streaming SSR?`, start from user-visible behavior and work inward to API/DB.

## Q127. Cache HTML?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Cache HTML?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Cache HTML?`, start from user-visible behavior and work inward to API/DB.

## Q128. Auth on SSR?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Auth on SSR?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Auth on SSR?`, start from user-visible behavior and work inward to API/DB.

## Q129. SEO for dashboards?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: SEO for dashboards?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `SEO for dashboards?`, start from user-visible behavior and work inward to API/DB.

## Q130. Spring serving SPA?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Spring serving SPA?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Spring serving SPA?`, start from user-visible behavior and work inward to API/DB.

## Q131. CDN cache cookies?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: CDN cache cookies?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `CDN cache cookies?`, start from user-visible behavior and work inward to API/DB.

## Q132. BOT rendering?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: BOT rendering?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `BOT rendering?`, start from user-visible behavior and work inward to API/DB.

# Server Components

## Q133. Client component boundary?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Client component boundary?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Client component boundary?`, start from user-visible behavior and work inward to API/DB.

## Q134. Passing props serializable?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Passing props serializable?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Passing props serializable?`, start from user-visible behavior and work inward to API/DB.

## Q135. When not to use RSC?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: When not to use RSC?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `When not to use RSC?`, start from user-visible behavior and work inward to API/DB.

## Q136. Data fetching on server?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Data fetching on server?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Data fetching on server?`, start from user-visible behavior and work inward to API/DB.

## Q137. Mixing SPA island?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Mixing SPA island?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Mixing SPA island?`, start from user-visible behavior and work inward to API/DB.

## Q138. RSC with Spring?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: RSC with Spring?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `RSC with Spring?`, start from user-visible behavior and work inward to API/DB.

## Q139. Bundle impact?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Bundle impact?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Bundle impact?`, start from user-visible behavior and work inward to API/DB.

## Q140. Caching RSC payloads?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Caching RSC payloads?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Caching RSC payloads?`, start from user-visible behavior and work inward to API/DB.

# Real-time

## Q141. SSE auth options?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: SSE auth options?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `SSE auth options?`, start from user-visible behavior and work inward to API/DB.

## Q142. Reconnect strategy?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Reconnect strategy?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Reconnect strategy?`, start from user-visible behavior and work inward to API/DB.

## Q143. Fan-out at scale?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Fan-out at scale?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Fan-out at scale?`, start from user-visible behavior and work inward to API/DB.

## Q144. Backpressure?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Backpressure?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Backpressure?`, start from user-visible behavior and work inward to API/DB.

## Q145. Exactly-once UI?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Exactly-once UI?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Exactly-once UI?`, start from user-visible behavior and work inward to API/DB.

## Q146. Poll fallback?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Poll fallback?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Poll fallback?`, start from user-visible behavior and work inward to API/DB.

## Q147. WebSocket sticky sessions?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: WebSocket sticky sessions?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `WebSocket sticky sessions?`, start from user-visible behavior and work inward to API/DB.

## Q148. Kafka role?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Kafka role?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Kafka role?`, start from user-visible behavior and work inward to API/DB.

# React + Spring Boot

## Q149. DTO mapping?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: DTO mapping?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `DTO mapping?`, start from user-visible behavior and work inward to API/DB.

## Q150. Validation errors 400 shape?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Validation errors 400 shape?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Validation errors 400 shape?`, start from user-visible behavior and work inward to API/DB.

## Q151. Multipart?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Multipart?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Multipart?`, start from user-visible behavior and work inward to API/DB.

## Q152. Actuator vs UI health?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Actuator vs UI health?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Actuator vs UI health?`, start from user-visible behavior and work inward to API/DB.

## Q153. Session fixation?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Session fixation?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Session fixation?`, start from user-visible behavior and work inward to API/DB.

## Q154. CORS credentials?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: CORS credentials?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `CORS credentials?`, start from user-visible behavior and work inward to API/DB.

## Q155. Method security?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Method security?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Method security?`, start from user-visible behavior and work inward to API/DB.

## Q156. Trace propagation?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Trace propagation?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Trace propagation?`, start from user-visible behavior and work inward to API/DB.

# Senior/Staff Architecture

## Q157. 20 services anti-pattern?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: 20 services anti-pattern?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `20 services anti-pattern?`, start from user-visible behavior and work inward to API/DB.

## Q158. 1M rows design?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: 1M rows design?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `1M rows design?`, start from user-visible behavior and work inward to API/DB.

## Q159. Multi-tenant UI?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Multi-tenant UI?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Multi-tenant UI?`, start from user-visible behavior and work inward to API/DB.

## Q160. Degraded mode UX?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Degraded mode UX?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Degraded mode UX?`, start from user-visible behavior and work inward to API/DB.

## Q161. Compliance audit trail?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Compliance audit trail?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Compliance audit trail?`, start from user-visible behavior and work inward to API/DB.

## Q162. Zero-downtime FE?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Zero-downtime FE?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Zero-downtime FE?`, start from user-visible behavior and work inward to API/DB.

## Q163. Platform team boundaries?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Platform team boundaries?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Platform team boundaries?`, start from user-visible behavior and work inward to API/DB.

## Q164. Hiring bar for FE in backend teams?

**30 SECOND ANSWER**

Answer in terms of problem → mechanism → failure mode. Tie to Payment Ops dashboard when possible.

**CODE**

```tsx
// See frontend/src and backend/src for concrete patterns related to: Hiring bar for FE in backend teams?
```

**DIAGRAM**

```text
Requirement → Architecture → Code → Failure → Perf → Security → Test
```

**WHY**

Interviewers probe whether you can connect UI behavior to Spring Boot contracts and production constraints.

**COMMON MISTAKE**

Framework-feature claimed as core React; or frontend-only security.

**FOLLOW-UP**

How would this change under 10x traffic or PCI constraints?

**SENIOR-LEVEL ANSWER**

I'd implement the smallest correct design, enforce authorization on the server, measure before memoizing, and document idempotency for payment side effects. For `Hiring bar for FE in backend teams?`, start from user-visible behavior and work inward to API/DB.

# Extra rapid-fire (complete 150+)

## Q165. What is hydration mismatch?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q166. How do keys interact with CSS transitions?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q167. Explain selectStatus in Redux?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q168. What is a render prop today?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q169. HOC vs hook migration?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q170. How does Vite HMR relate to Fast Refresh?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q171. Describe empty state design?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q172. Skeleton vs spinner?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q173. How to cancel fetch on unmount?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q174. What is request waterfalls?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q175. How to parallelize GETs?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q176. Prefer PATCH or PUT for status?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q177. How to version mobile clients?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q178. What is a design token?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q179. Explain aria-live for status?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q180. How do you handle offline forms?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q181. What is deterministic UI testing?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q182. Explain canary FE assets?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q183. How does CDN cache-busting work?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q184. What is SRI?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q185. Describe permission-aware navigation?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q186. How to hide PII in screenshots?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q187. What is differential loading?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q188. Explain import maps briefly?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q189. When use formAction?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q190. What is useFormStatus?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q191. Explain use() hook reading promise?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q192. How do Error Boundaries report?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q193. createRoot onCaughtError?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.

## Q194. What is owner stack React 19.1?

**30 SECOND ANSWER**

State the mechanism, one code sketch, one production caveat.

**CODE**

```tsx
// Lab: ConceptsLabPage / apiClient / PaymentDetailPage
```

**DIAGRAM**

```text
UI → apiClient → Spring → DB
```

**WHY / MISTAKE / FOLLOW-UP / SENIOR**

Prefer evidence from profiler/logs; never claim frontend authZ is enough; for payments insist on idempotency and audit.
