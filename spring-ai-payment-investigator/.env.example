# Optional — only needed for OpenAI provider or Docker infra
OPENAI_API_KEY=disabled

# Docker infra (optional profile)
POSTGRES_URL=jdbc:postgresql://localhost:5433/payments
POSTGRES_USER=sapi
POSTGRES_PASSWORD=sapi
KAFKA_BOOTSTRAP=localhost:9092
REDIS_HOST=localhost
REDIS_PORT=6379

# App
APP_AI_PROVIDER=scripted
