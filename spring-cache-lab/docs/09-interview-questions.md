# 09 — Interview cheat sheet

```text
CACHE = WHERE + WHAT + WHEN + HOW + FAILURE
Spring Cache → CacheManager → Provider (Caffeine local | Redis distributed)
@Cacheable=check first  @CachePut=refresh  @CacheEvict=forget
condition=BEFORE  unless=AFTER
DB = truth · Cache = optimization
```

See blog hub `/spring-cache` for 45 Q&As.
