# Legacy static pages

These were previously at the repository root and got published by GitHub Pages’
branch deploy, racing with (and sometimes overwriting) the Next.js `out/`
deploy from `.github/workflows/deploy.yml`.

Kept here for reference only — not part of the live Next.js site.
