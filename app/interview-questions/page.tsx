import Link from 'next/link'; import {getAllPosts} from '@/lib/posts';
const qs=[['What happens if Kafka goes down?','Discuss producer behavior, buffering, retries, backpressure and durability.'],['How do you prevent duplicate processing?','Use idempotency keys, deterministic business operations and state checks.'],['How would you scale 10×?','Find the first bottleneck, quantify it, then scale the constrained resource.'],['What if Redis is unavailable?','Protect the database from a cache-miss storm and define degraded behavior.'],['How do you choose SQL vs NoSQL?','Start with access patterns, consistency, transactions, scale and operational constraints.'],['What happens during a database failure?','Discuss replication, failover, timeouts, retry budgets and recovery.']];
export default function Interview(){const posts=getAllPosts();return <main className="mx-auto max-w-7xl px-5 py-14"><div className="max-w-3xl"><div className="text-xs font-black uppercase tracking-[.16em] text-blue-600">Interview Questions</div><h1 className="mt-3 text-5xl font-black tracking-[-.05em]">Practice the follow-up, not just the diagram.</h1><p className="mt-5 text-lg leading-8 text-slate-600">Senior interviews probe failure, scale, consistency and trade-offs. Use these questions after each design.</p></div><div className="mt-10 grid gap-4 md:grid-cols-2">{qs.map(([q,a])=><div className="card p-6" key={q}><h2 className="font-bold">{q}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{a}</p></div>)}</div><section className="mt-14"><h2 className="text-2xl font-black">Read a full design</h2><div className="mt-5 grid gap-4 md:grid-cols-3">{posts.slice(0,6).map(p=><Link className="card p-5" href={`/system-design/${p.slug}`} key={p.slug}><div className="font-bold">{p.title}</div><div className="mt-2 text-xs text-slate-500">{p.tags.join(' · ')}</div></Link>)}</div></section></main>}