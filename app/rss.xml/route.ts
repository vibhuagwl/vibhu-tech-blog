import {getAllPosts} from '@/lib/posts';
export const dynamic='force-static';
export async function GET(){const base='https://vibhuagwl.github.io/vibhu-tech-blog';const items=getAllPosts().map(p=>`<item><title><![CDATA[${p.title}]]></title><link>${base}/system-design/${p.slug}</link><guid>${base}/system-design/${p.slug}</guid><description><![CDATA[${p.description}]]></description><pubDate>${new Date(p.publishedAt).toUTCString()}</pubDate></item>`).join('');const xml=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>System Design Interview Hub</title><link>${base}</link><description>System design, distributed systems and FinTech engineering interview preparation.</description>${items}</channel></rss>`;return new Response(xml,{headers:{'Content-Type':'application/rss+xml; charset=utf-8'}})}
