import {getAllPosts} from '@/lib/posts'; import SearchClient from '@/components/search-client';
export default function Search(){return <main className="mx-auto max-w-5xl px-5 py-14"><h1 className="text-4xl font-black tracking-tight">Search the knowledge hub</h1><p className="mt-3 text-slate-500">Find systems, technologies, concepts and interview topics.</p><div className="mt-7"><SearchClient posts={getAllPosts().map(({slug,title,description,category,difficulty,tags})=>({slug,title,description,category,difficulty,tags}))}/></div></main>}