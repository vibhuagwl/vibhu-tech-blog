import Link from 'next/link'; import {getAllPosts} from '@/lib/posts';
export default function Learn(){const posts=getAllPosts();const fundamentals=posts.filter(p=>p.category==='Fundamentals');return <main className="mx-auto max-w-7xl px-5 py-14"><div className="max-w-3xl"><div className="text-xs font-black uppercase tracking-[.16em] text-blue-600">Learning Path</div><h1 className="mt-3 text-5xl font-black tracking-[-.05em]">From fundamentals to Staff-level system design.</h1><p className="mt-5 text-lg leading-8 text-slate-600">Follow the path in order or jump directly to a topic. Each article is written around decisions, bottlenecks, failure modes and trade-offs.</p></div><div className="mt-12 space-y-12"><section><h2 className="text-2xl font-black">Level 1 — Fundamentals</h2><div className="mt-5 grid gap-4 md:grid-cols-3">{fundamentals.map(p=><Link key={p.slug} href={`/system-design/${p.slug}`} className="card p-5"><div className="text-xs font-bold text-blue-600">{p.difficulty}</div><h3 className="mt-2 font-bold">{p.title}</h3><p className="mt-2 text-sm text-slate-500">{p.description}</p></Link>)}</div></section><section id="lld"><h2 className="text-2xl font-black">Level 2 — Building Blocks</h2><div className="mt-5 grid gap-4 md:grid-cols-3">{posts.filter(p=>['Infrastructure','Caching','Messaging'].includes(p.category)).map(p=><Link key={p.slug} href={`/system-design/${p.slug}`} className="card p-5"><div className="text-xs font-bold text-blue-600">{p.category}</div><h3 className="mt-2 font-bold">{p.title}</h3><p className="mt-2 text-sm text-slate-500">{p.description}</p></Link>)}</div></section><section id="databases"><h2 className="text-2xl font-black">Level 3 — Distributed Systems</h2><div className="mt-5 grid gap-4 md:grid-cols-3">{posts.filter(p=>p.category==='Distributed Systems').map(p=><Link key={p.slug} href={`/system-design/${p.slug}`} className="card p-5"><h3 className="font-bold">{p.title}</h3><p className="mt-2 text-sm text-slate-500">{p.description}</p></Link>)}</div></section></div></main>}