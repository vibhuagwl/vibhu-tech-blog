import Link from 'next/link'; import {getAllPosts} from '@/lib/posts';
export default function Distributed(){const posts=getAllPosts().filter(p=>['Distributed Systems','Caching','Messaging'].includes(p.category));return <main className="mx-auto max-w-7xl px-5 py-14"><div className="max-w-3xl"><div className="text-xs font-black uppercase tracking-[.16em] text-blue-600">Distributed Systems</div><h1 className="mt-3 text-5xl font-black tracking-[-.05em]">Reason about scale, failure and consistency.</h1><p className="mt-5 text-lg leading-8 text-slate-600">Core building blocks for senior backend interviews: caching, messaging, partitioning, replication, idempotency and resilience.</p></div><div className="mt-12 grid gap-4 md:grid-cols-3">{posts.map(p=><Link href={`/system-design/${p.slug}`} key={p.slug} className="card p-6"><div className="text-[10px] font-black uppercase text-blue-600">{p.category} · {p.difficulty}</div><h2 className="mt-3 text-xl font-bold">{p.title}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{p.description}</p></Link>)}</div></main>}