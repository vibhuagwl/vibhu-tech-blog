import Link from 'next/link'; import {getAllPosts} from '@/lib/posts';
export const metadata={title:'System Design Problems'};
export default function SystemDesign(){const posts=getAllPosts();return <main className="mx-auto max-w-7xl px-5 py-14"><div className="max-w-3xl"><div className="text-xs font-black uppercase tracking-[.16em] text-blue-600">System Design</div><h1 className="mt-3 text-5xl font-black tracking-[-.05em]">Design systems from first principles.</h1><p className="mt-5 text-lg leading-8 text-slate-600">Every problem follows the interview progression: requirements → estimation → simple design → bottlenecks → final architecture → reliability → trade-offs → Staff-level follow-ups.</p></div><div className="mt-12 grid gap-4 md:grid-cols-3">{posts.map(p=><Link key={p.slug} href={`/system-design/${p.slug}`} className="card p-6"><div className="text-[10px] font-black uppercase tracking-wider text-blue-600">{p.difficulty} · {p.readingTime}</div><h2 className="mt-3 text-xl font-bold">{p.title}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{p.description}</p><div className="mt-4 flex flex-wrap gap-1">{p.tags.map(t=><span key={t} className="rounded-full bg-slate-100 px-2 py-1 text-[10px] font-semibold text-slate-600">{t}</span>)}</div></Link>)}</div></main>}