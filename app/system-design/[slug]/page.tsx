import {notFound} from 'next/navigation'; import {compileMDX} from 'next-mdx-remote/rsc'; import {getAllPosts,getPost} from '@/lib/posts'; import {mdxComponents} from '@/components/mdx';
export async function generateStaticParams(){return getAllPosts().map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getPost(slug);return p?{title:p.title,description:p.description,alternates:{canonical:`/system-design/${p.slug}`},openGraph:{title:p.title,description:p.description,type:'article'}}:{}}
export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getPost(slug);if(!p)notFound();const {content}=await compileMDX({source:p.content,components:mdxComponents});const related=getAllPosts().filter(x=>x.slug!==p.slug&&x.tags.some(t=>p.tags.includes(t))).slice(0,3);return <main className="mx-auto max-w-7xl px-5 py-12"><div className="grid gap-12 lg:grid-cols-[minmax(0,820px)_240px]"><article><div className="text-xs font-black uppercase tracking-[.16em] text-blue-600">{p.category} · {p.difficulty}</div><h1 className="mt-4 text-5xl font-black tracking-[-.055em] md:text-6xl">{p.title}</h1><p className="mt-5 text-xl leading-8 text-slate-600">{p.description}</p><div className="mt-5 text-xs text-slate-400">{p.readingTime} · {p.publishedAt} · {p.tags.join(' · ')}</div><div className="prose-design mt-12">{content}</div></article><aside className="hidden lg:block"><div className="sticky top-24 card p-5"><div className="text-xs font-black uppercase tracking-wider text-slate-500">On this page</div><ul className="mt-4 space-y-2 text-sm text-slate-500"><li>Problem & Requirements</li><li>Estimation</li><li>Architecture</li><li>Data & APIs</li><li>Scaling</li><li>Reliability</li><li>Trade-offs</li><li>Follow-ups</li></ul></div></aside></div>{related.length>0&&<section className="mt-16 border-t pt-10"><h2 className="text-2xl font-black">Continue Learning</h2><div className="mt-5 grid gap-4 md:grid-cols-3">{related.map(r=><a key={r.slug} href={`/system-design/${r.slug}`} className="card p-5"><div className="text-[10px] font-black uppercase text-blue-600">{r.category}</div><div className="mt-2 font-bold">{r.title}</div></a>)}</div></section>}</main>}