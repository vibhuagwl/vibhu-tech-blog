import type {TocItem} from './types';

/** Interview map + spoken answers — sizing/monitoring/incidents live on /kafka-infra. */
export const KAFKA_MASTERY_TOC: TocItem[] = [
  {id: 'curriculum', label: '00. Interview map'},
  {id: 'interview', label: '01. Spoken answers'},
];
