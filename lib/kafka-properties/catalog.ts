import type {PropertyRow} from './types';

export const PRODUCER_CONFIGS: PropertyRow[] = [
  {name: `bootstrap.servers`, type: `list`, defaultValue: `""`, importance: 'high', purpose: `A list of host/port pairs used to establish the initial connection to the Kafka cluster. Clients use this list to bootstrap and discover the full set of…`, incomplete: true},
  {name: `buffer.memory`, type: `long`, defaultValue: `33554432`, importance: 'high', purpose: `The total bytes of memory the producer can use to buffer records waiting to be sent to the server. If records are sent faster than they can be delivered to…`, incomplete: true},
  {name: `compression.type`, type: `string`, defaultValue: `none`, importance: 'high', purpose: `The compression type for all data generated by the producer. The default is none (i.e. no compression). Valid values are none, gzip, snappy, lz4, or zstd.…`, incomplete: true},
  {name: `key.serializer`, type: `class`, defaultValue: `—`, importance: 'high', purpose: `Serializer class for key that implements the \`org.apache.kafka.common.serialization.Serializer\` interface.`, incomplete: false},
  {name: `ssl.key.password`, type: `password`, defaultValue: `null`, importance: 'high', purpose: `The password of the private key in the key store file or the PEM key specified in 'ssl.keystore.key'.`, incomplete: false},
  {name: `ssl.keystore.certificate.chain`, type: `password`, defaultValue: `null`, importance: 'high', purpose: `Certificate chain in the format specified by 'ssl.keystore.type'. Default SSL engine factory supports only PEM format with a list of X.509 certificates`, incomplete: false},
  {name: `ssl.keystore.key`, type: `password`, defaultValue: `null`, importance: 'high', purpose: `Private key in the format specified by 'ssl.keystore.type'. Default SSL engine factory supports only PEM format with PKCS#8 keys. If the key is encrypted,…`, incomplete: true},
  {name: `ssl.keystore.location`, type: `string`, defaultValue: `null`, importance: 'high', purpose: `The location of the key store file. This is optional for client and can be used for two-way authentication for client.`, incomplete: false},
  {name: `ssl.keystore.password`, type: `password`, defaultValue: `null`, importance: 'high', purpose: `The store password for the key store file. This is optional for client and only needed if 'ssl.keystore.location' is configured. Key store password is not…`, incomplete: true},
  {name: `ssl.truststore.certificates`, type: `password`, defaultValue: `null`, importance: 'high', purpose: `Trusted certificates in the format specified by 'ssl.truststore.type'. Default SSL engine factory supports only PEM format with X.509 certificates.`, incomplete: false},
  {name: `ssl.truststore.location`, type: `string`, defaultValue: `null`, importance: 'high', purpose: `The location of the trust store file.`, incomplete: false},
  {name: `ssl.truststore.password`, type: `password`, defaultValue: `null`, importance: 'high', purpose: `The password for the trust store file. If a password is not set, trust store file configured will still be used, but integrity checking is disabled. Trust…`, incomplete: true},
  {name: `value.serializer`, type: `class`, defaultValue: `—`, importance: 'high', purpose: `Serializer class for value that implements the \`org.apache.kafka.common.serialization.Serializer\` interface.`, incomplete: false},
  {name: `batch.size`, type: `int`, defaultValue: `16384`, importance: 'medium', purpose: `The producer will attempt to batch records together into fewer requests whenever multiple records are being sent to the same partition. This helps…`, incomplete: true},
  {name: `client.dns.lookup`, type: `string`, defaultValue: `use_all_dns_ips`, importance: 'medium', purpose: `Controls how the client uses DNS lookups. If set to \`use_all_dns_ips, connect to each returned IP address in sequence until a successful connection is…`, incomplete: true},
  {name: `client.id`, type: `string`, defaultValue: `""`, importance: 'medium', purpose: `An id string to pass to the server when making requests. The purpose of this is to be able to track the source of requests beyond just ip/port by allowing…`, incomplete: true},
  {name: `compression.gzip.level`, type: `int`, defaultValue: `-1`, importance: 'medium', purpose: `The compression level to use if compression.type is set to \`gzip.`, incomplete: false},
  {name: `compression.lz4.level`, type: `int`, defaultValue: `9`, importance: 'medium', purpose: `The compression level to use if compression.type is set to \`lz4.`, incomplete: false},
  {name: `compression.zstd.level`, type: `int`, defaultValue: `3`, importance: 'medium', purpose: `The compression level to use if compression.type is set to \`zstd.`, incomplete: false},
  {name: `connections.max.idle.ms`, type: `long`, defaultValue: `540000 (9 minutes)`, importance: 'medium', purpose: `Close idle connections after the number of milliseconds specified by this config.`, incomplete: false},
  {name: `delivery.timeout.ms`, type: `int`, defaultValue: `120000 (2 minutes)`, importance: 'medium', purpose: `An upper bound on the time to report success or failure after a call tosend() returns. This limits the total time that a record will be delayed prior to…`, incomplete: true},
  {name: `linger.ms`, type: `long`, defaultValue: `5`, importance: 'medium', purpose: `The producer groups together any records that arrive in between request transmissions into a single batched request. Normally this occurs only under load…`, incomplete: true},
  {name: `max.block.ms`, type: `long`, defaultValue: `60000 (1 minute)`, importance: 'medium', purpose: `The configuration controls how long theKafkaProducer'ssend(),partitionsFor(),initTransactions(),sendOffsetsToTransaction(),commitTransaction()…`, incomplete: true},
  {name: `max.request.size`, type: `int`, defaultValue: `1048576`, importance: 'medium', purpose: `The maximum size of a request in bytes. This setting will limit the number of record batches the producer will send in a single request to avoid sending…`, incomplete: true},
  {name: `partitioner.class`, type: `class`, defaultValue: `null`, importance: 'medium', purpose: `Determines which partition to send a record to when records are produced. Available options are: Implementing the…`, incomplete: true},
  {name: `partitioner.ignore.keys`, type: `boolean`, defaultValue: `false`, importance: 'medium', purpose: `When set to 'true' the producer won't use record keys to choose a partition. If 'false', producer would choose a partition based on a hash of the key when…`, incomplete: true},
  {name: `receive.buffer.bytes`, type: `int`, defaultValue: `32768 (32 kibibytes)`, importance: 'medium', purpose: `The size of the TCP receive buffer (SO_RCVBUF) to use when reading data. If the value is -1, the OS default will be used.`, incomplete: false},
  {name: `request.timeout.ms`, type: `int`, defaultValue: `30000 (30 seconds)`, importance: 'medium', purpose: `The configuration controls the maximum amount of time the client will wait for the response of a request. If the response is not received before the…`, incomplete: true},
  {name: `sasl.client.callback.handler.class`, type: `class`, defaultValue: `null`, importance: 'medium', purpose: `The fully qualified name of a SASL client callback handler class that implements the AuthenticateCallbackHandler interface.`, incomplete: false},
  {name: `sasl.jaas.config`, type: `password`, defaultValue: `null`, importance: 'medium', purpose: `JAAS login context parameters for SASL connections in the format used by JAAS configuration files. JAAS configuration file format is described here. The…`, incomplete: true},
  {name: `sasl.kerberos.service.name`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `The Kerberos principal name that Kafka runs as. This can be defined either in Kafka's JAAS config or in Kafka's config.`, incomplete: false},
  {name: `sasl.login.callback.handler.class`, type: `class`, defaultValue: `null`, importance: 'medium', purpose: `The fully qualified name of a SASL login callback handler class that implements the AuthenticateCallbackHandler interface. For brokers, login callback…`, incomplete: true},
  {name: `sasl.login.class`, type: `class`, defaultValue: `null`, importance: 'medium', purpose: `The fully qualified name of a class that implements the Login interface. For brokers, login config must be prefixed with listener prefix and SASL mechanism…`, incomplete: true},
  {name: `sasl.mechanism`, type: `string`, defaultValue: `GSSAPI`, importance: 'medium', purpose: `SASL mechanism used for client connections. This may be any mechanism for which a security provider is available. GSSAPI is the default mechanism.`, incomplete: false},
  {name: `sasl.oauthbearer.jwks.endpoint.url`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `The OAuth/OIDC provider URL from which the provider's JWKS (JSON Web Key Set) can be retrieved. The URL can be HTTP(S)-based or file-based. If the URL is…`, incomplete: true},
  {name: `sasl.oauthbearer.token.endpoint.url`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `The URL for the OAuth/OIDC identity provider. If the URL is HTTP(S)-based, it is the issuer's token endpoint URL to which requests will be made to login…`, incomplete: true},
  {name: `security.protocol`, type: `string`, defaultValue: `PLAINTEXT`, importance: 'medium', purpose: `Protocol used to communicate with brokers.`, incomplete: false},
  {name: `send.buffer.bytes`, type: `int`, defaultValue: `131072 (128 kibibytes)`, importance: 'medium', purpose: `The size of the TCP send buffer (SO_SNDBUF) to use when sending data. If the value is -1, the OS default will be used.`, incomplete: false},
  {name: `socket.connection.setup.timeout.max.ms`, type: `long`, defaultValue: `30000 (30 seconds)`, importance: 'medium', purpose: `The maximum amount of time the client will wait for the socket connection to be established. The connection setup timeout will increase exponentially for…`, incomplete: true},
  {name: `socket.connection.setup.timeout.ms`, type: `long`, defaultValue: `10000 (10 seconds)`, importance: 'medium', purpose: `The amount of time the client will wait for the socket connection to be established. If the connection is not built before the timeout elapses, clients…`, incomplete: true},
  {name: `ssl.enabled.protocols`, type: `list`, defaultValue: `TLSv1.2,TLSv1.3`, importance: 'medium', purpose: `The list of protocols enabled for SSL connections. The default is 'TLSv1.2,TLSv1.3'. This means that clients and servers will prefer TLSv1.3 if both…`, incomplete: true},
  {name: `ssl.keystore.type`, type: `string`, defaultValue: `JKS`, importance: 'medium', purpose: `The file format of the key store file. This is optional for client. The values currently supported by the default ssl.engine.factory.class are [JKS,…`, incomplete: true},
  {name: `ssl.protocol`, type: `string`, defaultValue: `TLSv1.3`, importance: 'medium', purpose: `The SSL protocol used to generate the SSLContext. The default is 'TLSv1.3', which should be fine for most use cases. A typical alternative to the default…`, incomplete: true},
  {name: `ssl.provider`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `The name of the security provider used for SSL connections. Default value is the default security provider of the JVM.`, incomplete: false},
  {name: `ssl.truststore.type`, type: `string`, defaultValue: `JKS`, importance: 'medium', purpose: `The file format of the trust store file. The values currently supported by the default ssl.engine.factory.class are [JKS, PKCS12, PEM].`, incomplete: false},
  {name: `acks`, type: `string`, defaultValue: `all`, importance: 'low', purpose: `The number of acknowledgments the producer requires the leader to have received before considering a request complete. This controls the durability of…`, incomplete: true},
  {name: `enable.idempotence`, type: `boolean`, defaultValue: `true`, importance: 'low', purpose: `When set to 'true', the producer will ensure that exactly one copy of each message is written in the stream. If 'false', producer retries due to broker…`, incomplete: true},
  {name: `enable.metrics.push`, type: `boolean`, defaultValue: `true`, importance: 'low', purpose: `Whether to enable pushing of client metrics to the cluster, if the cluster has a client metrics subscription which matches this client.`, incomplete: false},
  {name: `interceptor.classes`, type: `list`, defaultValue: `""`, importance: 'low', purpose: `A list of classes to use as interceptors. Implementing the \`org.apache.kafka.clients.producer.ProducerInterceptor interface allows you to intercept (and…`, incomplete: true},
  {name: `max.in.flight.requests.per.connection`, type: `int`, defaultValue: `5`, importance: 'low', purpose: `The maximum number of unacknowledged requests the client will send on a single connection before blocking. Note that if this configuration is set to be…`, incomplete: true},
  {name: `metadata.max.age.ms`, type: `long`, defaultValue: `300000 (5 minutes)`, importance: 'low', purpose: `The period of time in milliseconds after which we force a refresh of metadata even if we haven't seen any partition leadership changes to proactively…`, incomplete: true},
  {name: `metadata.max.idle.ms`, type: `long`, defaultValue: `300000 (5 minutes)`, importance: 'low', purpose: `Controls how long the producer will cache metadata for a topic that's idle. If the elapsed time since a topic was last produced to exceeds the metadata…`, incomplete: true},
  {name: `metadata.recovery.rebootstrap.trigger.ms`, type: `long`, defaultValue: `300000 (5 minutes)`, importance: 'low', purpose: `If a client configured to rebootstrap usingmetadata.recovery.strategy=rebootstrap is unable to obtain metadata from any of the brokers in the last known…`, incomplete: true},
  {name: `metadata.recovery.strategy`, type: `string`, defaultValue: `rebootstrap`, importance: 'low', purpose: `Controls how the client recovers when none of the brokers known to it is available. If set to \`none, the client fails. If set to \`rebootstrap, the client…`, incomplete: true},
  {name: `metric.reporters`, type: `list`, defaultValue: `org.apache.kafka.common.metrics.JmxReporter`, importance: 'low', purpose: `A list of classes to use as metrics reporters. Implementing the \`org.apache.kafka.common.metrics.MetricsReporter interface allows plugging in classes that…`, incomplete: true},
  {name: `metrics.num.samples`, type: `int`, defaultValue: `2`, importance: 'low', purpose: `The number of samples maintained to compute metrics.`, incomplete: false},
  {name: `metrics.recording.level`, type: `string`, defaultValue: `INFO`, importance: 'low', purpose: `The highest recording level for metrics. It has three levels for recording metrics - info, debug, and trace.INFO level records only essential metrics…`, incomplete: true},
  {name: `metrics.sample.window.ms`, type: `long`, defaultValue: `30000 (30 seconds)`, importance: 'low', purpose: `The window of time a metrics sample is computed over.`, incomplete: false},
  {name: `partitioner.adaptive.partitioning.enable`, type: `boolean`, defaultValue: `true`, importance: 'low', purpose: `When set to 'true', the producer will try to adapt to broker performance and produce more messages to partitions hosted on faster brokers. If 'false',…`, incomplete: true},
  {name: `partitioner.availability.timeout.ms`, type: `long`, defaultValue: `0`, importance: 'low', purpose: `If a broker cannot process produce requests from a partition forpartitioner.availability.timeout.ms time, the partitioner treats that partition as not…`, incomplete: true},
  {name: `reconnect.backoff.max.ms`, type: `long`, defaultValue: `1000 (1 second)`, importance: 'low', purpose: `The maximum amount of time in milliseconds to wait when reconnecting to a broker that has repeatedly failed to connect. If provided, the backoff per host…`, incomplete: true},
  {name: `reconnect.backoff.ms`, type: `long`, defaultValue: `50`, importance: 'low', purpose: `The base amount of time to wait before attempting to reconnect to a given host. This avoids repeatedly connecting to a host in a tight loop. This backoff…`, incomplete: true},
  {name: `retry.backoff.max.ms`, type: `long`, defaultValue: `1000 (1 second)`, importance: 'low', purpose: `The maximum amount of time in milliseconds to wait when retrying a request to the broker that has repeatedly failed. If provided, the backoff per client…`, incomplete: true},
  {name: `retry.backoff.ms`, type: `long`, defaultValue: `100`, importance: 'low', purpose: `The amount of time to wait before attempting to retry a failed request to a given topic partition. This avoids repeatedly sending requests in a tight loop…`, incomplete: true},
  {name: `sasl.kerberos.kinit.cmd`, type: `string`, defaultValue: `/usr/bin/kinit`, importance: 'low', purpose: `Kerberos kinit command path.`, incomplete: false},
  {name: `sasl.kerberos.min.time.before.relogin`, type: `long`, defaultValue: `60000`, importance: 'low', purpose: `Login thread sleep time between refresh attempts.`, incomplete: false},
  {name: `sasl.kerberos.ticket.renew.jitter`, type: `double`, defaultValue: `0.05`, importance: 'low', purpose: `Percentage of random jitter added to the renewal time.`, incomplete: false},
  {name: `sasl.kerberos.ticket.renew.window.factor`, type: `double`, defaultValue: `0.8`, importance: 'low', purpose: `Login thread will sleep until the specified window factor of time from last refresh to ticket's expiry has been reached, at which time it will try to renew…`, incomplete: true},
  {name: `sasl.login.connect.timeout.ms`, type: `int`, defaultValue: `null`, importance: 'low', purpose: `The (optional) value in milliseconds for the external authentication provider connection timeout. Currently applies only to OAUTHBEARER.`, incomplete: false},
  {name: `sasl.login.read.timeout.ms`, type: `int`, defaultValue: `null`, importance: 'low', purpose: `The (optional) value in milliseconds for the external authentication provider read timeout. Currently applies only to OAUTHBEARER.`, incomplete: false},
  {name: `sasl.login.refresh.buffer.seconds`, type: `short`, defaultValue: `300`, importance: 'low', purpose: `The amount of buffer time before credential expiration to maintain when refreshing a credential, in seconds. If a refresh would otherwise occur closer to…`, incomplete: true},
  {name: `sasl.login.refresh.min.period.seconds`, type: `short`, defaultValue: `60`, importance: 'low', purpose: `The desired minimum time for the login refresh thread to wait before refreshing a credential, in seconds. Legal values are between 0 and 900 (15 minutes);…`, incomplete: true},
  {name: `sasl.login.refresh.window.factor`, type: `double`, defaultValue: `0.8`, importance: 'low', purpose: `Login refresh thread will sleep until the specified window factor relative to the credential's lifetime has been reached, at which time it will try to…`, incomplete: true},
  {name: `sasl.login.refresh.window.jitter`, type: `double`, defaultValue: `0.05`, importance: 'low', purpose: `The maximum amount of random jitter relative to the credential's lifetime that is added to the login refresh thread's sleep time. Legal values are between…`, incomplete: true},
  {name: `sasl.login.retry.backoff.max.ms`, type: `long`, defaultValue: `10000 (10 seconds)`, importance: 'low', purpose: `The (optional) value in milliseconds for the maximum wait between login attempts to the external authentication provider. Login uses an exponential backoff…`, incomplete: true},
  {name: `sasl.login.retry.backoff.ms`, type: `long`, defaultValue: `100`, importance: 'low', purpose: `The (optional) value in milliseconds for the initial wait between login attempts to the external authentication provider. Login uses an exponential backoff…`, incomplete: true},
  {name: `sasl.oauthbearer.clock.skew.seconds`, type: `int`, defaultValue: `30`, importance: 'low', purpose: `The (optional) value in seconds to allow for differences between the time of the OAuth/OIDC identity provider and the broker.`, incomplete: false},
  {name: `sasl.oauthbearer.expected.audience`, type: `list`, defaultValue: `null`, importance: 'low', purpose: `The (optional) comma-delimited setting for the broker to use to verify that the JWT was issued for one of the expected audiences. The JWT will be inspected…`, incomplete: true},
  {name: `sasl.oauthbearer.expected.issuer`, type: `string`, defaultValue: `null`, importance: 'low', purpose: `The (optional) setting for the broker to use to verify that the JWT was created by the expected issuer. The JWT will be inspected for the standard OAuth…`, incomplete: true},
  {name: `sasl.oauthbearer.header.urlencode`, type: `boolean`, defaultValue: `false`, importance: 'low', purpose: `The (optional) setting to enable the OAuth client to URL-encode the client_id and client_secret in the authorization header in accordance with RFC6749, see…`, incomplete: true},
  {name: `sasl.oauthbearer.jwks.endpoint.refresh.ms`, type: `long`, defaultValue: `3600000 (1 hour)`, importance: 'low', purpose: `The (optional) value in milliseconds for the broker to wait between refreshing its JWKS (JSON Web Key Set) cache that contains the keys to verify the…`, incomplete: true},
  {name: `sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms`, type: `long`, defaultValue: `10000 (10 seconds)`, importance: 'low', purpose: `The (optional) value in milliseconds for the maximum wait between attempts to retrieve the JWKS (JSON Web Key Set) from the external authentication provider.`, incomplete: true},
  {name: `sasl.oauthbearer.jwks.endpoint.retry.backoff.ms`, type: `long`, defaultValue: `100`, importance: 'low', purpose: `The (optional) value in milliseconds for the initial wait between JWKS (JSON Web Key Set) retrieval attempts from the external authentication provider.…`, incomplete: true},
  {name: `sasl.oauthbearer.scope.claim.name`, type: `string`, defaultValue: `scope`, importance: 'low', purpose: `The OAuth claim for the scope is often named "scope", but this (optional) setting can provide a different name to use for the scope included in the JWT…`, incomplete: true},
  {name: `sasl.oauthbearer.sub.claim.name`, type: `string`, defaultValue: `sub`, importance: 'low', purpose: `The OAuth claim for the subject is often named "sub", but this (optional) setting can provide a different name to use for the subject included in the JWT…`, incomplete: true},
  {name: `security.providers`, type: `string`, defaultValue: `null`, importance: 'low', purpose: `A list of configurable creator classes each returning a provider implementing security algorithms. These classes should implement the \`org.apache.kafka.common.`, incomplete: true},
  {name: `ssl.cipher.suites`, type: `list`, defaultValue: `null`, importance: 'low', purpose: `A list of cipher suites. This is a named combination of authentication, encryption, MAC and key exchange algorithm used to negotiate the security settings…`, incomplete: true},
  {name: `ssl.endpoint.identification.algorithm`, type: `string`, defaultValue: `https`, importance: 'low', purpose: `The endpoint identification algorithm to validate server hostname using server certificate.`, incomplete: false},
  {name: `ssl.engine.factory.class`, type: `class`, defaultValue: `null`, importance: 'low', purpose: `The class of type org.apache.kafka.common.security.auth.SslEngineFactory to provide SSLEngine objects. Default value is org.apache.kafka.common.security.ssl.`, incomplete: true},
  {name: `ssl.keymanager.algorithm`, type: `string`, defaultValue: `SunX509`, importance: 'low', purpose: `The algorithm used by key manager factory for SSL connections. Default value is the key manager factory algorithm configured for the Java Virtual Machine.`, incomplete: false},
  {name: `ssl.secure.random.implementation`, type: `string`, defaultValue: `null`, importance: 'low', purpose: `The SecureRandom PRNG implementation to use for SSL cryptography operations.`, incomplete: false},
  {name: `ssl.trustmanager.algorithm`, type: `string`, defaultValue: `PKIX`, importance: 'low', purpose: `The algorithm used by trust manager factory for SSL connections. Default value is the trust manager factory algorithm configured for the Java Virtual Machine.`, incomplete: false},
  {name: `transaction.timeout.ms`, type: `int`, defaultValue: `60000 (1 minute)`, importance: 'low', purpose: `The maximum amount of time in milliseconds that a transaction will remain open before the coordinator proactively aborts it. The start of the transaction…`, incomplete: true},
  {name: `transactional.id`, type: `string`, defaultValue: `null`, importance: 'low', purpose: `The TransactionalId to use for transactional delivery. This enables reliability semantics which span multiple producer sessions since it allows the client…`, incomplete: true},
];

export const CONSUMER_CONFIGS: PropertyRow[] = [
  {name: `bootstrap.servers`, type: `list`, defaultValue: `""`, importance: 'high', purpose: `A list of host/port pairs used to establish the initial connection to the Kafka cluster. Clients use this list to bootstrap and discover the full set of…`, incomplete: true},
  {name: `fetch.min.bytes`, type: `int`, defaultValue: `1`, importance: 'high', purpose: `The minimum amount of data the server should return for a fetch request. If insufficient data is available the request will wait for that much data to…`, incomplete: true},
  {name: `group.id`, type: `string`, defaultValue: `null`, importance: 'high', purpose: `A unique string that identifies the consumer group this consumer belongs to. This property is required if the consumer uses either the group management…`, incomplete: true},
  {name: `group.protocol`, type: `string`, defaultValue: `classic`, importance: 'high', purpose: `The group protocol consumer should use. We currently support "classic" or "consumer". If "consumer" is specified, then the consumer group protocol will be…`, incomplete: true},
  {name: `heartbeat.interval.ms`, type: `int`, defaultValue: `3000 (3 seconds)`, importance: 'high', purpose: `The expected time between heartbeats to the consumer coordinator when using Kafka's group management facilities. Heartbeats are used to ensure that the…`, incomplete: true},
  {name: `key.deserializer`, type: `class`, defaultValue: `—`, importance: 'high', purpose: `Deserializer class for key that implements the \`org.apache.kafka.common.serialization.Deserializer interface.`, incomplete: false},
  {name: `max.partition.fetch.bytes`, type: `int`, defaultValue: `1048576 (1 mebibyte)`, importance: 'high', purpose: `The maximum amount of data per-partition the server will return. Records are fetched in batches by the consumer. If the first record batch in the first…`, incomplete: true},
  {name: `session.timeout.ms`, type: `int`, defaultValue: `45000 (45 seconds)`, importance: 'high', purpose: `The timeout used to detect client failures when using Kafka's group management facility. The client sends periodic heartbeats to indicate its liveness to…`, incomplete: true},
  {name: `ssl.key.password`, type: `password`, defaultValue: `null`, importance: 'high', purpose: `The password of the private key in the key store file or the PEM key specified in 'ssl.keystore.key'.`, incomplete: false},
  {name: `ssl.keystore.certificate.chain`, type: `password`, defaultValue: `null`, importance: 'high', purpose: `Certificate chain in the format specified by 'ssl.keystore.type'. Default SSL engine factory supports only PEM format with a list of X.509 certificates`, incomplete: false},
  {name: `ssl.keystore.key`, type: `password`, defaultValue: `null`, importance: 'high', purpose: `Private key in the format specified by 'ssl.keystore.type'. Default SSL engine factory supports only PEM format with PKCS#8 keys. If the key is encrypted,…`, incomplete: true},
  {name: `ssl.keystore.location`, type: `string`, defaultValue: `null`, importance: 'high', purpose: `The location of the key store file. This is optional for client and can be used for two-way authentication for client.`, incomplete: false},
  {name: `ssl.keystore.password`, type: `password`, defaultValue: `null`, importance: 'high', purpose: `The store password for the key store file. This is optional for client and only needed if 'ssl.keystore.location' is configured. Key store password is not…`, incomplete: true},
  {name: `ssl.truststore.certificates`, type: `password`, defaultValue: `null`, importance: 'high', purpose: `Trusted certificates in the format specified by 'ssl.truststore.type'. Default SSL engine factory supports only PEM format with X.509 certificates.`, incomplete: false},
  {name: `ssl.truststore.location`, type: `string`, defaultValue: `null`, importance: 'high', purpose: `The location of the trust store file.`, incomplete: false},
  {name: `ssl.truststore.password`, type: `password`, defaultValue: `null`, importance: 'high', purpose: `The password for the trust store file. If a password is not set, trust store file configured will still be used, but integrity checking is disabled. Trust…`, incomplete: true},
  {name: `value.deserializer`, type: `class`, defaultValue: `—`, importance: 'high', purpose: `Deserializer class for value that implements the \`org.apache.kafka.common.serialization.Deserializer interface.`, incomplete: false},
  {name: `allow.auto.create.topics`, type: `boolean`, defaultValue: `true`, importance: 'medium', purpose: `Allow automatic topic creation on the broker when subscribing to or assigning a topic. A topic being subscribed to will be automatically created only if…`, incomplete: true},
  {name: `auto.offset.reset`, type: `string`, defaultValue: `latest`, importance: 'medium', purpose: `What to do when there is no initial offset in Kafka or if the current offset does not exist any more on the server (e.g. because that data has been…`, incomplete: true},
  {name: `client.dns.lookup`, type: `string`, defaultValue: `use_all_dns_ips`, importance: 'medium', purpose: `Controls how the client uses DNS lookups. If set to \`use_all_dns_ips, connect to each returned IP address in sequence until a successful connection is…`, incomplete: true},
  {name: `connections.max.idle.ms`, type: `long`, defaultValue: `540000 (9 minutes)`, importance: 'medium', purpose: `Close idle connections after the number of milliseconds specified by this config.`, incomplete: false},
  {name: `default.api.timeout.ms`, type: `int`, defaultValue: `60000 (1 minute)`, importance: 'medium', purpose: `Specifies the timeout (in milliseconds) for client APIs. This configuration is used as the default timeout for all client operations that do not specify…`, incomplete: true},
  {name: `enable.auto.commit`, type: `boolean`, defaultValue: `true`, importance: 'medium', purpose: `If true the consumer's offset will be periodically committed in the background.`, incomplete: false},
  {name: `exclude.internal.topics`, type: `boolean`, defaultValue: `true`, importance: 'medium', purpose: `Whether internal topics matching a subscribed pattern should be excluded from the subscription. It is always possible to explicitly subscribe to an…`, incomplete: true},
  {name: `fetch.max.bytes`, type: `int`, defaultValue: `52428800 (50 mebibytes)`, importance: 'medium', purpose: `The maximum amount of data the server should return for a fetch request. Records are fetched in batches by the consumer, and if the first record batch in…`, incomplete: true},
  {name: `group.instance.id`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `A unique identifier of the consumer instance provided by the end user. Only non-empty strings are permitted. If set, the consumer is treated as a static…`, incomplete: true},
  {name: `group.remote.assignor`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `The name of the server-side assignor to use. If not specified, the group coordinator will pick the first assignor defined in the broker config…`, incomplete: true},
  {name: `isolation.level`, type: `string`, defaultValue: `read_uncommitted`, importance: 'medium', purpose: `Controls how to read messages written transactionally. If set to \`read_committed, consumer.poll() will only return transactional messages which have been…`, incomplete: true},
  {name: `max.poll.interval.ms`, type: `int`, defaultValue: `300000 (5 minutes)`, importance: 'medium', purpose: `The maximum delay between invocations of poll() when using consumer group management. This places an upper bound on the amount of time that the consumer…`, incomplete: true},
  {name: `max.poll.records`, type: `int`, defaultValue: `500`, importance: 'medium', purpose: `The maximum number of records returned in a single call to poll(). Note, thatmax.poll.records does not impact the underlying fetching behavior. The…`, incomplete: true},
  {name: `partition.assignment.strategy`, type: `list`, defaultValue: `class org.apache.kafka.clients.consumer.RangeAssignor,class org.apache.kafka.clients.consumer.CooperativeStickyAssignor`, importance: 'medium', purpose: `A list of class names or class types, ordered by preference, of supported partition assignment strategies that the client will use to distribute partition…`, incomplete: true},
  {name: `receive.buffer.bytes`, type: `int`, defaultValue: `65536 (64 kibibytes)`, importance: 'medium', purpose: `The size of the TCP receive buffer (SO_RCVBUF) to use when reading data. If the value is -1, the OS default will be used.`, incomplete: false},
  {name: `request.timeout.ms`, type: `int`, defaultValue: `30000 (30 seconds)`, importance: 'medium', purpose: `The configuration controls the maximum amount of time the client will wait for the response of a request. If the response is not received before the…`, incomplete: true},
  {name: `sasl.client.callback.handler.class`, type: `class`, defaultValue: `null`, importance: 'medium', purpose: `The fully qualified name of a SASL client callback handler class that implements the AuthenticateCallbackHandler interface.`, incomplete: false},
  {name: `sasl.jaas.config`, type: `password`, defaultValue: `null`, importance: 'medium', purpose: `JAAS login context parameters for SASL connections in the format used by JAAS configuration files. JAAS configuration file format is described here. The…`, incomplete: true},
  {name: `sasl.kerberos.service.name`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `The Kerberos principal name that Kafka runs as. This can be defined either in Kafka's JAAS config or in Kafka's config.`, incomplete: false},
  {name: `sasl.login.callback.handler.class`, type: `class`, defaultValue: `null`, importance: 'medium', purpose: `The fully qualified name of a SASL login callback handler class that implements the AuthenticateCallbackHandler interface. For brokers, login callback…`, incomplete: true},
  {name: `sasl.login.class`, type: `class`, defaultValue: `null`, importance: 'medium', purpose: `The fully qualified name of a class that implements the Login interface. For brokers, login config must be prefixed with listener prefix and SASL mechanism…`, incomplete: true},
  {name: `sasl.mechanism`, type: `string`, defaultValue: `GSSAPI`, importance: 'medium', purpose: `SASL mechanism used for client connections. This may be any mechanism for which a security provider is available. GSSAPI is the default mechanism.`, incomplete: false},
  {name: `sasl.oauthbearer.jwks.endpoint.url`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `The OAuth/OIDC provider URL from which the provider's JWKS (JSON Web Key Set) can be retrieved. The URL can be HTTP(S)-based or file-based. If the URL is…`, incomplete: true},
  {name: `sasl.oauthbearer.token.endpoint.url`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `The URL for the OAuth/OIDC identity provider. If the URL is HTTP(S)-based, it is the issuer's token endpoint URL to which requests will be made to login…`, incomplete: true},
  {name: `security.protocol`, type: `string`, defaultValue: `PLAINTEXT`, importance: 'medium', purpose: `Protocol used to communicate with brokers.`, incomplete: false},
  {name: `send.buffer.bytes`, type: `int`, defaultValue: `131072 (128 kibibytes)`, importance: 'medium', purpose: `The size of the TCP send buffer (SO_SNDBUF) to use when sending data. If the value is -1, the OS default will be used.`, incomplete: false},
  {name: `socket.connection.setup.timeout.max.ms`, type: `long`, defaultValue: `30000 (30 seconds)`, importance: 'medium', purpose: `The maximum amount of time the client will wait for the socket connection to be established. The connection setup timeout will increase exponentially for…`, incomplete: true},
  {name: `socket.connection.setup.timeout.ms`, type: `long`, defaultValue: `10000 (10 seconds)`, importance: 'medium', purpose: `The amount of time the client will wait for the socket connection to be established. If the connection is not built before the timeout elapses, clients…`, incomplete: true},
  {name: `ssl.enabled.protocols`, type: `list`, defaultValue: `TLSv1.2,TLSv1.3`, importance: 'medium', purpose: `The list of protocols enabled for SSL connections. The default is 'TLSv1.2,TLSv1.3'. This means that clients and servers will prefer TLSv1.3 if both…`, incomplete: true},
  {name: `ssl.keystore.type`, type: `string`, defaultValue: `JKS`, importance: 'medium', purpose: `The file format of the key store file. This is optional for client. The values currently supported by the default ssl.engine.factory.class are [JKS,…`, incomplete: true},
  {name: `ssl.protocol`, type: `string`, defaultValue: `TLSv1.3`, importance: 'medium', purpose: `The SSL protocol used to generate the SSLContext. The default is 'TLSv1.3', which should be fine for most use cases. A typical alternative to the default…`, incomplete: true},
  {name: `ssl.provider`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `The name of the security provider used for SSL connections. Default value is the default security provider of the JVM.`, incomplete: false},
  {name: `ssl.truststore.type`, type: `string`, defaultValue: `JKS`, importance: 'medium', purpose: `The file format of the trust store file. The values currently supported by the default ssl.engine.factory.class are [JKS, PKCS12, PEM].`, incomplete: false},
  {name: `auto.commit.interval.ms`, type: `int`, defaultValue: `5000 (5 seconds)`, importance: 'low', purpose: `The frequency in milliseconds that the consumer offsets are auto-committed to Kafka If \`enable.auto.commit\` is set to \`true.`, incomplete: false},
  {name: `check.crcs`, type: `boolean`, defaultValue: `true`, importance: 'low', purpose: `Automatically check the CRC32 of the records consumed. This ensures no on-the-wire or on-disk corruption to the messages occurred. This check adds some…`, incomplete: true},
  {name: `client.id`, type: `string`, defaultValue: `""`, importance: 'low', purpose: `An id string to pass to the server when making requests. The purpose of this is to be able to track the source of requests beyond just ip/port by allowing…`, incomplete: true},
  {name: `client.rack`, type: `string`, defaultValue: `""`, importance: 'low', purpose: `A rack identifier for this client. This can be any string value which indicates where this client is physically located. It corresponds with the broker…`, incomplete: true},
  {name: `enable.metrics.push`, type: `boolean`, defaultValue: `true`, importance: 'low', purpose: `Whether to enable pushing of client metrics to the cluster, if the cluster has a client metrics subscription which matches this client.`, incomplete: false},
  {name: `fetch.max.wait.ms`, type: `int`, defaultValue: `500`, importance: 'low', purpose: `The maximum amount of time the server will block before answering the fetch request there isn't sufficient data to immediately satisfy the requirement…`, incomplete: true},
  {name: `interceptor.classes`, type: `list`, defaultValue: `""`, importance: 'low', purpose: `A list of classes to use as interceptors. Implementing the \`org.apache.kafka.clients.consumer.ConsumerInterceptor interface allows you to intercept (and…`, incomplete: true},
  {name: `metadata.max.age.ms`, type: `long`, defaultValue: `300000 (5 minutes)`, importance: 'low', purpose: `The period of time in milliseconds after which we force a refresh of metadata even if we haven't seen any partition leadership changes to proactively…`, incomplete: true},
  {name: `metadata.recovery.rebootstrap.trigger.ms`, type: `long`, defaultValue: `300000 (5 minutes)`, importance: 'low', purpose: `If a client configured to rebootstrap usingmetadata.recovery.strategy=rebootstrap is unable to obtain metadata from any of the brokers in the last known…`, incomplete: true},
  {name: `metadata.recovery.strategy`, type: `string`, defaultValue: `rebootstrap`, importance: 'low', purpose: `Controls how the client recovers when none of the brokers known to it is available. If set to \`none, the client fails. If set to \`rebootstrap, the client…`, incomplete: true},
  {name: `metric.reporters`, type: `list`, defaultValue: `org.apache.kafka.common.metrics.JmxReporter`, importance: 'low', purpose: `A list of classes to use as metrics reporters. Implementing the \`org.apache.kafka.common.metrics.MetricsReporter interface allows plugging in classes that…`, incomplete: true},
  {name: `metrics.num.samples`, type: `int`, defaultValue: `2`, importance: 'low', purpose: `The number of samples maintained to compute metrics.`, incomplete: false},
  {name: `metrics.recording.level`, type: `string`, defaultValue: `INFO`, importance: 'low', purpose: `The highest recording level for metrics. It has three levels for recording metrics - info, debug, and trace.INFO level records only essential metrics…`, incomplete: true},
  {name: `metrics.sample.window.ms`, type: `long`, defaultValue: `30000 (30 seconds)`, importance: 'low', purpose: `The window of time a metrics sample is computed over.`, incomplete: false},
  {name: `reconnect.backoff.max.ms`, type: `long`, defaultValue: `1000 (1 second)`, importance: 'low', purpose: `The maximum amount of time in milliseconds to wait when reconnecting to a broker that has repeatedly failed to connect. If provided, the backoff per host…`, incomplete: true},
  {name: `reconnect.backoff.ms`, type: `long`, defaultValue: `50`, importance: 'low', purpose: `The base amount of time to wait before attempting to reconnect to a given host. This avoids repeatedly connecting to a host in a tight loop. This backoff…`, incomplete: true},
  {name: `retry.backoff.max.ms`, type: `long`, defaultValue: `1000 (1 second)`, importance: 'low', purpose: `The maximum amount of time in milliseconds to wait when retrying a request to the broker that has repeatedly failed. If provided, the backoff per client…`, incomplete: true},
  {name: `retry.backoff.ms`, type: `long`, defaultValue: `100`, importance: 'low', purpose: `The amount of time to wait before attempting to retry a failed request to a given topic partition. This avoids repeatedly sending requests in a tight loop…`, incomplete: true},
  {name: `sasl.kerberos.kinit.cmd`, type: `string`, defaultValue: `/usr/bin/kinit`, importance: 'low', purpose: `Kerberos kinit command path.`, incomplete: false},
  {name: `sasl.kerberos.min.time.before.relogin`, type: `long`, defaultValue: `60000`, importance: 'low', purpose: `Login thread sleep time between refresh attempts.`, incomplete: false},
  {name: `sasl.kerberos.ticket.renew.jitter`, type: `double`, defaultValue: `0.05`, importance: 'low', purpose: `Percentage of random jitter added to the renewal time.`, incomplete: false},
  {name: `sasl.kerberos.ticket.renew.window.factor`, type: `double`, defaultValue: `0.8`, importance: 'low', purpose: `Login thread will sleep until the specified window factor of time from last refresh to ticket's expiry has been reached, at which time it will try to renew…`, incomplete: true},
  {name: `sasl.login.connect.timeout.ms`, type: `int`, defaultValue: `null`, importance: 'low', purpose: `The (optional) value in milliseconds for the external authentication provider connection timeout. Currently applies only to OAUTHBEARER.`, incomplete: false},
  {name: `sasl.login.read.timeout.ms`, type: `int`, defaultValue: `null`, importance: 'low', purpose: `The (optional) value in milliseconds for the external authentication provider read timeout. Currently applies only to OAUTHBEARER.`, incomplete: false},
  {name: `sasl.login.refresh.buffer.seconds`, type: `short`, defaultValue: `300`, importance: 'low', purpose: `The amount of buffer time before credential expiration to maintain when refreshing a credential, in seconds. If a refresh would otherwise occur closer to…`, incomplete: true},
  {name: `sasl.login.refresh.min.period.seconds`, type: `short`, defaultValue: `60`, importance: 'low', purpose: `The desired minimum time for the login refresh thread to wait before refreshing a credential, in seconds. Legal values are between 0 and 900 (15 minutes);…`, incomplete: true},
  {name: `sasl.login.refresh.window.factor`, type: `double`, defaultValue: `0.8`, importance: 'low', purpose: `Login refresh thread will sleep until the specified window factor relative to the credential's lifetime has been reached, at which time it will try to…`, incomplete: true},
  {name: `sasl.login.refresh.window.jitter`, type: `double`, defaultValue: `0.05`, importance: 'low', purpose: `The maximum amount of random jitter relative to the credential's lifetime that is added to the login refresh thread's sleep time. Legal values are between…`, incomplete: true},
  {name: `sasl.login.retry.backoff.max.ms`, type: `long`, defaultValue: `10000 (10 seconds)`, importance: 'low', purpose: `The (optional) value in milliseconds for the maximum wait between login attempts to the external authentication provider. Login uses an exponential backoff…`, incomplete: true},
  {name: `sasl.login.retry.backoff.ms`, type: `long`, defaultValue: `100`, importance: 'low', purpose: `The (optional) value in milliseconds for the initial wait between login attempts to the external authentication provider. Login uses an exponential backoff…`, incomplete: true},
  {name: `sasl.oauthbearer.clock.skew.seconds`, type: `int`, defaultValue: `30`, importance: 'low', purpose: `The (optional) value in seconds to allow for differences between the time of the OAuth/OIDC identity provider and the broker.`, incomplete: false},
  {name: `sasl.oauthbearer.expected.audience`, type: `list`, defaultValue: `null`, importance: 'low', purpose: `The (optional) comma-delimited setting for the broker to use to verify that the JWT was issued for one of the expected audiences. The JWT will be inspected…`, incomplete: true},
  {name: `sasl.oauthbearer.expected.issuer`, type: `string`, defaultValue: `null`, importance: 'low', purpose: `The (optional) setting for the broker to use to verify that the JWT was created by the expected issuer. The JWT will be inspected for the standard OAuth…`, incomplete: true},
  {name: `sasl.oauthbearer.header.urlencode`, type: `boolean`, defaultValue: `false`, importance: 'low', purpose: `The (optional) setting to enable the OAuth client to URL-encode the client_id and client_secret in the authorization header in accordance with RFC6749, see…`, incomplete: true},
  {name: `sasl.oauthbearer.jwks.endpoint.refresh.ms`, type: `long`, defaultValue: `3600000 (1 hour)`, importance: 'low', purpose: `The (optional) value in milliseconds for the broker to wait between refreshing its JWKS (JSON Web Key Set) cache that contains the keys to verify the…`, incomplete: true},
  {name: `sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms`, type: `long`, defaultValue: `10000 (10 seconds)`, importance: 'low', purpose: `The (optional) value in milliseconds for the maximum wait between attempts to retrieve the JWKS (JSON Web Key Set) from the external authentication provider.`, incomplete: true},
  {name: `sasl.oauthbearer.jwks.endpoint.retry.backoff.ms`, type: `long`, defaultValue: `100`, importance: 'low', purpose: `The (optional) value in milliseconds for the initial wait between JWKS (JSON Web Key Set) retrieval attempts from the external authentication provider.…`, incomplete: true},
  {name: `sasl.oauthbearer.scope.claim.name`, type: `string`, defaultValue: `scope`, importance: 'low', purpose: `The OAuth claim for the scope is often named "scope", but this (optional) setting can provide a different name to use for the scope included in the JWT…`, incomplete: true},
  {name: `sasl.oauthbearer.sub.claim.name`, type: `string`, defaultValue: `sub`, importance: 'low', purpose: `The OAuth claim for the subject is often named "sub", but this (optional) setting can provide a different name to use for the subject included in the JWT…`, incomplete: true},
  {name: `security.providers`, type: `string`, defaultValue: `null`, importance: 'low', purpose: `A list of configurable creator classes each returning a provider implementing security algorithms. These classes should implement the \`org.apache.kafka.common.`, incomplete: true},
  {name: `ssl.cipher.suites`, type: `list`, defaultValue: `null`, importance: 'low', purpose: `A list of cipher suites. This is a named combination of authentication, encryption, MAC and key exchange algorithm used to negotiate the security settings…`, incomplete: true},
  {name: `ssl.endpoint.identification.algorithm`, type: `string`, defaultValue: `https`, importance: 'low', purpose: `The endpoint identification algorithm to validate server hostname using server certificate.`, incomplete: false},
  {name: `ssl.engine.factory.class`, type: `class`, defaultValue: `null`, importance: 'low', purpose: `The class of type org.apache.kafka.common.security.auth.SslEngineFactory to provide SSLEngine objects. Default value is org.apache.kafka.common.security.ssl.`, incomplete: true},
  {name: `ssl.keymanager.algorithm`, type: `string`, defaultValue: `SunX509`, importance: 'low', purpose: `The algorithm used by key manager factory for SSL connections. Default value is the key manager factory algorithm configured for the Java Virtual Machine.`, incomplete: false},
  {name: `ssl.secure.random.implementation`, type: `string`, defaultValue: `null`, importance: 'low', purpose: `The SecureRandom PRNG implementation to use for SSL cryptography operations.`, incomplete: false},
  {name: `ssl.trustmanager.algorithm`, type: `string`, defaultValue: `PKIX`, importance: 'low', purpose: `The algorithm used by trust manager factory for SSL connections. Default value is the trust manager factory algorithm configured for the Java Virtual Machine.`, incomplete: false},
];

export const CONTROLLER_CONFIGS: PropertyRow[] = [
  {name: `controller.listener.names`, type: `string`, defaultValue: `null`, importance: 'high', purpose: `A comma-separated list of the names of the listeners used by the controller. This is required when communicating with the controller quorum, the broker…`, incomplete: true},
  {name: `controller.quorum.bootstrap.servers`, type: `list`, defaultValue: `""`, importance: 'high', purpose: `List of endpoints to use for bootstrapping the cluster metadata. The endpoints are specified in comma-separated list of \`{host}:{port} entries. For example:…`, incomplete: true},
  {name: `controller.quorum.election.backoff.max.ms`, type: `int`, defaultValue: `1000 (1 second)`, importance: 'high', purpose: `Maximum time in milliseconds before starting new elections. This is used in the binary exponential backoff mechanism that helps prevent gridlocked elections`, incomplete: false},
  {name: `controller.quorum.election.timeout.ms`, type: `int`, defaultValue: `1000 (1 second)`, importance: 'high', purpose: `Maximum time in milliseconds to wait without being able to fetch from the leader before triggering a new election`, incomplete: false},
  {name: `controller.quorum.fetch.timeout.ms`, type: `int`, defaultValue: `2000 (2 seconds)`, importance: 'high', purpose: `Maximum time without a successful fetch from the current leader before becoming a candidate and triggering an election for voters; Maximum time a leader…`, incomplete: true},
  {name: `controller.quorum.voters`, type: `list`, defaultValue: `""`, importance: 'high', purpose: `Map of id/endpoint information for the set of voters in a comma-separated list of \`{id}@{host}:{port} entries. For example:…`, incomplete: true},
  {name: `group.coordinator.threads`, type: `int`, defaultValue: `4`, importance: 'high', purpose: `The number of threads used by the group coordinator.`, incomplete: false},
  {name: `metadata.log.dir`, type: `string`, defaultValue: `null`, importance: 'high', purpose: `This configuration determines where we put the metadata log. If it is not set, the metadata log is placed in the first log directory from \`log.dirs\`.`, incomplete: false},
  {name: `metadata.log.max.record.bytes.between.snapshots`, type: `long`, defaultValue: `20971520`, importance: 'high', purpose: `This is the maximum number of bytes in the log between the latest snapshot and the high-watermark needed before generating a new snapshot. The default…`, incomplete: true},
  {name: `metadata.log.max.snapshot.interval.ms`, type: `long`, defaultValue: `3600000 (1 hour)`, importance: 'high', purpose: `This is the maximum number of milliseconds to wait to generate a snapshot if there are committed records in the log that are not included in the latest…`, incomplete: true},
  {name: `metadata.log.segment.bytes`, type: `int`, defaultValue: `1073741824 (1 gibibyte)`, importance: 'high', purpose: `The maximum size of a single metadata log file.`, incomplete: false},
  {name: `metadata.log.segment.ms`, type: `long`, defaultValue: `604800000 (7 days)`, importance: 'high', purpose: `The maximum time before a new metadata log file is rolled out (in milliseconds).`, incomplete: false},
  {name: `metadata.max.retention.bytes`, type: `long`, defaultValue: `104857600 (100 mebibytes)`, importance: 'high', purpose: `The maximum combined size of the metadata log and snapshots before deleting old snapshots and log files. Since at least one snapshot must exist before any…`, incomplete: true},
  {name: `metadata.max.retention.ms`, type: `long`, defaultValue: `604800000 (7 days)`, importance: 'high', purpose: `The number of milliseconds to keep a metadata log file or snapshot before deleting it. Since at least one snapshot must exist before any logs can be…`, incomplete: true},
  {name: `node.id`, type: `int`, defaultValue: `—`, importance: 'high', purpose: `The node ID associated with the roles this process is playing when \`process.roles\` is non-empty. This is required configuration when running in KRaft mode.`, incomplete: false},
  {name: `offsets.topic.compression.codec`, type: `int`, defaultValue: `0`, importance: 'high', purpose: `Compression codec for the offsets topic - compression may be used to achieve "atomic" commits.`, incomplete: false},
  {name: `offsets.topic.num.partitions`, type: `int`, defaultValue: `50`, importance: 'high', purpose: `The number of partitions for the offset commit topic (should not change after deployment).`, incomplete: false},
  {name: `offsets.topic.replication.factor`, type: `short`, defaultValue: `3`, importance: 'high', purpose: `The replication factor for the offsets topic (set higher to ensure availability). Internal topic creation will fail until the cluster size meets this…`, incomplete: true},
  {name: `offsets.topic.segment.bytes`, type: `int`, defaultValue: `104857600 (100 mebibytes)`, importance: 'high', purpose: `The offsets topic segment bytes should be kept relatively small in order to facilitate faster log compaction and cache loads.`, incomplete: false},
  {name: `process.roles`, type: `list`, defaultValue: `—`, importance: 'high', purpose: `The roles that this process plays: 'broker', 'controller', or 'broker,controller' if it is both.`, incomplete: false},
  {name: `transaction.state.log.load.buffer.size`, type: `int`, defaultValue: `5242880`, importance: 'high', purpose: `Batch size for reading from the transaction log segments when loading producer ids and transactions into the cache (soft-limit, overridden if records are…`, incomplete: true},
  {name: `transaction.state.log.min.isr`, type: `int`, defaultValue: `2`, importance: 'high', purpose: `The minimum number of replicas that must acknowledge a write to transaction topic in order to be considered successful.`, incomplete: false},
  {name: `transaction.state.log.num.partitions`, type: `int`, defaultValue: `50`, importance: 'high', purpose: `The number of partitions for the transaction topic (should not change after deployment).`, incomplete: false},
  {name: `transaction.state.log.replication.factor`, type: `short`, defaultValue: `3`, importance: 'high', purpose: `The replication factor for the transaction topic (set higher to ensure availability). Internal topic creation will fail until the cluster size meets this…`, incomplete: true},
  {name: `transaction.state.log.segment.bytes`, type: `int`, defaultValue: `104857600 (100 mebibytes)`, importance: 'high', purpose: `The transaction topic segment bytes should be kept relatively small in order to facilitate faster log compaction and cache loads`, incomplete: false},
  {name: `controller.quorum.append.linger.ms`, type: `int`, defaultValue: `25`, importance: 'medium', purpose: `The duration in milliseconds that the leader will wait for writes to accumulate before flushing them to disk.`, incomplete: false},
  {name: `controller.quorum.request.timeout.ms`, type: `int`, defaultValue: `2000 (2 seconds)`, importance: 'medium', purpose: `The configuration controls the maximum amount of time the client will wait for the response of a request. If the response is not received before the…`, incomplete: true},
  {name: `controller.socket.timeout.ms`, type: `int`, defaultValue: `30000 (30 seconds)`, importance: 'medium', purpose: `The socket timeout for controller-to-broker channels.`, incomplete: false},
  {name: `group.consumer.assignors`, type: `list`, defaultValue: `uniform,range`, importance: 'medium', purpose: `The server side assignors as a list of either names for builtin assignors or full class names for customer assignors. The first one in the list is…`, incomplete: true},
  {name: `group.consumer.heartbeat.interval.ms`, type: `int`, defaultValue: `5000 (5 seconds)`, importance: 'medium', purpose: `The heartbeat interval given to the members of a consumer group.`, incomplete: false},
  {name: `group.consumer.max.heartbeat.interval.ms`, type: `int`, defaultValue: `15000 (15 seconds)`, importance: 'medium', purpose: `The maximum heartbeat interval for registered consumers.`, incomplete: false},
  {name: `group.consumer.max.session.timeout.ms`, type: `int`, defaultValue: `60000 (1 minute)`, importance: 'medium', purpose: `The maximum allowed session timeout for registered consumers.`, incomplete: false},
  {name: `group.consumer.max.size`, type: `int`, defaultValue: `2147483647`, importance: 'medium', purpose: `The maximum number of consumers that a single consumer group can accommodate. This value will only impact groups under the CONSUMER group protocol. To…`, incomplete: true},
  {name: `group.consumer.migration.policy`, type: `string`, defaultValue: `bidirectional`, importance: 'medium', purpose: `The config that enables converting the non-empty classic group using the consumer embedded protocol to the non-empty consumer group using the consumer…`, incomplete: true},
  {name: `group.consumer.min.heartbeat.interval.ms`, type: `int`, defaultValue: `5000 (5 seconds)`, importance: 'medium', purpose: `The minimum heartbeat interval for registered consumers.`, incomplete: false},
  {name: `group.consumer.min.session.timeout.ms`, type: `int`, defaultValue: `45000 (45 seconds)`, importance: 'medium', purpose: `The minimum allowed session timeout for registered consumers.`, incomplete: false},
  {name: `group.consumer.session.timeout.ms`, type: `int`, defaultValue: `45000 (45 seconds)`, importance: 'medium', purpose: `The timeout to detect client failures when using the consumer group protocol.`, incomplete: false},
  {name: `group.coordinator.append.linger.ms`, type: `int`, defaultValue: `5`, importance: 'medium', purpose: `The duration in milliseconds that the coordinator will wait for writes to accumulate before flushing them to disk. Transactional writes are not accumulated.`, incomplete: false},
  {name: `group.coordinator.rebalance.protocols`, type: `list`, defaultValue: `classic,consumer`, importance: 'medium', purpose: `The list of enabled rebalance protocols.The share rebalance protocol is in early access and therefore must not be used in production.`, incomplete: false},
  {name: `controller.quorum.retry.backoff.ms`, type: `int`, defaultValue: `20`, importance: 'low', purpose: `The amount of time to wait before attempting to retry a failed request to a given topic partition. This avoids repeatedly sending requests in a tight loop…`, incomplete: true},
  {name: `controller.quota.window.num`, type: `int`, defaultValue: `11`, importance: 'low', purpose: `The number of samples to retain in memory for controller mutation quotas`, incomplete: false},
  {name: `controller.quota.window.size.seconds`, type: `int`, defaultValue: `1`, importance: 'low', purpose: `The time span of each sample for controller mutations quotas`, incomplete: false},
  {name: `metadata.max.idle.interval.ms`, type: `int`, defaultValue: `500`, importance: 'low', purpose: `This configuration controls how often the active controller should write no-op records to the metadata partition. If the value is 0, no-op records are not…`, incomplete: true},
];

export const BROKER_CONFIGS: PropertyRow[] = [
  {name: `add.partitions.to.txn.retry.backoff.max.ms`, type: `int`, defaultValue: `100`, importance: 'high', purpose: `The maximum allowed timeout for adding partitions to transactions on the server side. It only applies to the actual add partition operations, not the…`, incomplete: true},
  {name: `add.partitions.to.txn.retry.backoff.ms`, type: `int`, defaultValue: `20`, importance: 'high', purpose: `The server-side retry backoff when the server attemptsto add the partition to the transaction`, incomplete: false},
  {name: `advertised.listeners`, type: `string`, defaultValue: `null`, importance: 'high', purpose: `Specifies the listener addresses that the Kafka brokers will advertise to clients and other brokers. The config is useful where the actual listener…`, incomplete: true},
  {name: `auto.create.topics.enable`, type: `boolean`, defaultValue: `true`, importance: 'high', purpose: `Enable auto creation of topic on the server.`, incomplete: false},
  {name: `auto.leader.rebalance.enable`, type: `boolean`, defaultValue: `true`, importance: 'high', purpose: `Enables auto leader balancing. A background thread checks the distribution of partition leaders at regular intervals, configurable by leader.imbalance.check.`, incomplete: true},
  {name: `background.threads`, type: `int`, defaultValue: `10`, importance: 'high', purpose: `The number of threads to use for various background processing tasks`, incomplete: false},
  {name: `broker.id`, type: `int`, defaultValue: `-1`, importance: 'high', purpose: `The broker id for this server.`, incomplete: false},
  {name: `compression.type`, type: `string`, defaultValue: `producer`, importance: 'high', purpose: `Specify the final compression type for a given topic. This configuration accepts the standard compression codecs ('gzip', 'snappy', 'lz4', 'zstd'). It…`, incomplete: true},
  {name: `controller.listener.names`, type: `string`, defaultValue: `null`, importance: 'high', purpose: `A comma-separated list of the names of the listeners used by the controller. This is required when communicating with the controller quorum, the broker…`, incomplete: true},
  {name: `controller.quorum.bootstrap.servers`, type: `list`, defaultValue: `""`, importance: 'high', purpose: `List of endpoints to use for bootstrapping the cluster metadata. The endpoints are specified in comma-separated list of \`{host}:{port} entries. For example:…`, incomplete: true},
  {name: `controller.quorum.election.backoff.max.ms`, type: `int`, defaultValue: `1000 (1 second)`, importance: 'high', purpose: `Maximum time in milliseconds before starting new elections. This is used in the binary exponential backoff mechanism that helps prevent gridlocked elections`, incomplete: false},
  {name: `controller.quorum.election.timeout.ms`, type: `int`, defaultValue: `1000 (1 second)`, importance: 'high', purpose: `Maximum time in milliseconds to wait without being able to fetch from the leader before triggering a new election`, incomplete: false},
  {name: `controller.quorum.fetch.timeout.ms`, type: `int`, defaultValue: `2000 (2 seconds)`, importance: 'high', purpose: `Maximum time without a successful fetch from the current leader before becoming a candidate and triggering an election for voters; Maximum time a leader…`, incomplete: true},
  {name: `controller.quorum.voters`, type: `list`, defaultValue: `""`, importance: 'high', purpose: `Map of id/endpoint information for the set of voters in a comma-separated list of \`{id}@{host}:{port} entries. For example:…`, incomplete: true},
  {name: `delete.topic.enable`, type: `boolean`, defaultValue: `true`, importance: 'high', purpose: `Enables delete topic. Delete topic through the admin tool will have no effect if this config is turned off`, incomplete: false},
  {name: `early.start.listeners`, type: `string`, defaultValue: `null`, importance: 'high', purpose: `A comma-separated list of listener names which may be started before the authorizer has finished initialization. This is useful when the authorizer is…`, incomplete: true},
  {name: `group.coordinator.threads`, type: `int`, defaultValue: `4`, importance: 'high', purpose: `The number of threads used by the group coordinator.`, incomplete: false},
  {name: `leader.imbalance.check.interval.seconds`, type: `long`, defaultValue: `300`, importance: 'high', purpose: `The frequency with which the partition rebalance check is triggered by the controller`, incomplete: false},
  {name: `log.dir`, type: `string`, defaultValue: `/tmp/kafka-logs`, importance: 'high', purpose: `The directory in which the log data is kept (supplemental for log.dirs property)`, incomplete: false},
  {name: `log.dirs`, type: `string`, defaultValue: `null`, importance: 'high', purpose: `A comma-separated list of the directories where the log data is stored. If not set, the value in log.dir is used.`, incomplete: false},
  {name: `log.flush.interval.messages`, type: `long`, defaultValue: `9223372036854775807`, importance: 'high', purpose: `The number of messages accumulated on a log partition before messages are flushed to disk.`, incomplete: false},
  {name: `log.flush.interval.ms`, type: `long`, defaultValue: `null`, importance: 'high', purpose: `The maximum time in ms that a message in any topic is kept in memory before flushed to disk. If not set, the value in log.flush.scheduler.interval.ms is used`, incomplete: false},
  {name: `log.flush.offset.checkpoint.interval.ms`, type: `int`, defaultValue: `60000 (1 minute)`, importance: 'high', purpose: `The frequency with which we update the persistent record of the last flush which acts as the log recovery point.`, incomplete: false},
  {name: `log.flush.scheduler.interval.ms`, type: `long`, defaultValue: `9223372036854775807`, importance: 'high', purpose: `The frequency in ms that the log flusher checks whether any log needs to be flushed to disk`, incomplete: false},
  {name: `log.flush.start.offset.checkpoint.interval.ms`, type: `int`, defaultValue: `60000 (1 minute)`, importance: 'high', purpose: `The frequency with which we update the persistent record of log start offset`, incomplete: false},
  {name: `log.retention.bytes`, type: `long`, defaultValue: `-1`, importance: 'high', purpose: `The maximum size of the log before deleting it`, incomplete: false},
  {name: `log.retention.hours`, type: `int`, defaultValue: `168`, importance: 'high', purpose: `The number of hours to keep a log file before deleting it (in hours), tertiary to log.retention.ms property`, incomplete: false},
  {name: `log.retention.minutes`, type: `int`, defaultValue: `null`, importance: 'high', purpose: `The number of minutes to keep a log file before deleting it (in minutes), secondary to log.retention.ms property. If not set, the value in…`, incomplete: true},
  {name: `log.retention.ms`, type: `long`, defaultValue: `null`, importance: 'high', purpose: `The number of milliseconds to keep a log file before deleting it (in milliseconds), If not set, the value in log.retention.minutes is used. If set to -1,…`, incomplete: true},
  {name: `log.roll.hours`, type: `int`, defaultValue: `168`, importance: 'high', purpose: `The maximum time before a new log segment is rolled out (in hours), secondary to log.roll.ms property`, incomplete: false},
  {name: `log.roll.jitter.hours`, type: `int`, defaultValue: `0`, importance: 'high', purpose: `The maximum jitter to subtract from logRollTimeMillis (in hours), secondary to log.roll.jitter.ms property`, incomplete: false},
  {name: `log.roll.jitter.ms`, type: `long`, defaultValue: `null`, importance: 'high', purpose: `The maximum jitter to subtract from logRollTimeMillis (in milliseconds). If not set, the value in log.roll.jitter.hours is used`, incomplete: false},
  {name: `log.roll.ms`, type: `long`, defaultValue: `null`, importance: 'high', purpose: `The maximum time before a new log segment is rolled out (in milliseconds). If not set, the value in log.roll.hours is used`, incomplete: false},
  {name: `log.segment.bytes`, type: `int`, defaultValue: `1073741824 (1 gibibyte)`, importance: 'high', purpose: `The maximum size of a single log file`, incomplete: false},
  {name: `log.segment.delete.delay.ms`, type: `long`, defaultValue: `60000 (1 minute)`, importance: 'high', purpose: `The amount of time to wait before deleting a file from the filesystem. If the value is 0 and there is no file to delete, the system will wait 1 millisecond.`, incomplete: true},
  {name: `message.max.bytes`, type: `int`, defaultValue: `1048588`, importance: 'high', purpose: `The largest record batch size allowed by Kafka (after compression if compression is enabled). If this is increased and there are consumers older than…`, incomplete: true},
  {name: `metadata.log.dir`, type: `string`, defaultValue: `null`, importance: 'high', purpose: `This configuration determines where we put the metadata log. If it is not set, the metadata log is placed in the first log directory from \`log.dirs\`.`, incomplete: false},
  {name: `metadata.log.max.record.bytes.between.snapshots`, type: `long`, defaultValue: `20971520`, importance: 'high', purpose: `This is the maximum number of bytes in the log between the latest snapshot and the high-watermark needed before generating a new snapshot. The default…`, incomplete: true},
  {name: `metadata.log.max.snapshot.interval.ms`, type: `long`, defaultValue: `3600000 (1 hour)`, importance: 'high', purpose: `This is the maximum number of milliseconds to wait to generate a snapshot if there are committed records in the log that are not included in the latest…`, incomplete: true},
  {name: `metadata.log.segment.bytes`, type: `int`, defaultValue: `1073741824 (1 gibibyte)`, importance: 'high', purpose: `The maximum size of a single metadata log file.`, incomplete: false},
  {name: `metadata.log.segment.ms`, type: `long`, defaultValue: `604800000 (7 days)`, importance: 'high', purpose: `The maximum time before a new metadata log file is rolled out (in milliseconds).`, incomplete: false},
  {name: `metadata.max.retention.bytes`, type: `long`, defaultValue: `104857600 (100 mebibytes)`, importance: 'high', purpose: `The maximum combined size of the metadata log and snapshots before deleting old snapshots and log files. Since at least one snapshot must exist before any…`, incomplete: true},
  {name: `metadata.max.retention.ms`, type: `long`, defaultValue: `604800000 (7 days)`, importance: 'high', purpose: `The number of milliseconds to keep a metadata log file or snapshot before deleting it. Since at least one snapshot must exist before any logs can be…`, incomplete: true},
  {name: `min.insync.replicas`, type: `int`, defaultValue: `1`, importance: 'high', purpose: `When a producer sets acks to "all" (or "-1"), this configuration specifies the minimum number of replicas that must acknowledge a write for the write to be…`, incomplete: true},
  {name: `node.id`, type: `int`, defaultValue: `—`, importance: 'high', purpose: `The node ID associated with the roles this process is playing when \`process.roles\` is non-empty. This is required configuration when running in KRaft mode.`, incomplete: false},
  {name: `num.io.threads`, type: `int`, defaultValue: `8`, importance: 'high', purpose: `The number of threads that the server uses for processing requests, which may include disk I/O`, incomplete: false},
  {name: `num.network.threads`, type: `int`, defaultValue: `3`, importance: 'high', purpose: `The number of threads that the server uses for receiving requests from the network and sending responses to the network. Noted: each listener (except for…`, incomplete: true},
  {name: `num.recovery.threads.per.data.dir`, type: `int`, defaultValue: `2`, importance: 'high', purpose: `The number of threads per data directory to be used for log recovery at startup and flushing at shutdown`, incomplete: false},
  {name: `num.replica.alter.log.dirs.threads`, type: `int`, defaultValue: `null`, importance: 'high', purpose: `The number of threads that can move replicas between log directories, which may include disk I/O. The default value is equal to the number of directories…`, incomplete: true},
  {name: `num.replica.fetchers`, type: `int`, defaultValue: `1`, importance: 'high', purpose: `Number of fetcher threads used to replicate records from each source broker. The total number of fetchers on each broker is bound bynum.replica.fetchers…`, incomplete: true},
  {name: `offset.metadata.max.bytes`, type: `int`, defaultValue: `4096 (4 kibibytes)`, importance: 'high', purpose: `The maximum size for a metadata entry associated with an offset commit.`, incomplete: false},
  {name: `offsets.commit.timeout.ms`, type: `int`, defaultValue: `5000 (5 seconds)`, importance: 'high', purpose: `Offset commit will be delayed until all replicas for the offsets topic receive the commit or this timeout is reached. This is similar to the producer…`, incomplete: true},
  {name: `offsets.load.buffer.size`, type: `int`, defaultValue: `5242880`, importance: 'high', purpose: `Batch size for reading from the offsets segments when loading group metadata into the cache (soft-limit, overridden if records are too large).`, incomplete: false},
  {name: `offsets.retention.check.interval.ms`, type: `long`, defaultValue: `600000 (10 minutes)`, importance: 'high', purpose: `Frequency at which to check for stale offsets`, incomplete: false},
  {name: `offsets.retention.minutes`, type: `int`, defaultValue: `10080`, importance: 'high', purpose: `For subscribed consumers, committed offset of a specific partition will be expired and discarded when 1) this retention period has elapsed after the…`, incomplete: true},
  {name: `offsets.topic.compression.codec`, type: `int`, defaultValue: `0`, importance: 'high', purpose: `Compression codec for the offsets topic - compression may be used to achieve "atomic" commits.`, incomplete: false},
  {name: `offsets.topic.num.partitions`, type: `int`, defaultValue: `50`, importance: 'high', purpose: `The number of partitions for the offset commit topic (should not change after deployment).`, incomplete: false},
  {name: `offsets.topic.replication.factor`, type: `short`, defaultValue: `3`, importance: 'high', purpose: `The replication factor for the offsets topic (set higher to ensure availability). Internal topic creation will fail until the cluster size meets this…`, incomplete: true},
  {name: `offsets.topic.segment.bytes`, type: `int`, defaultValue: `104857600 (100 mebibytes)`, importance: 'high', purpose: `The offsets topic segment bytes should be kept relatively small in order to facilitate faster log compaction and cache loads.`, incomplete: false},
  {name: `process.roles`, type: `list`, defaultValue: `—`, importance: 'high', purpose: `The roles that this process plays: 'broker', 'controller', or 'broker,controller' if it is both.`, incomplete: false},
  {name: `queued.max.requests`, type: `int`, defaultValue: `500`, importance: 'high', purpose: `The number of queued requests allowed for data-plane, before blocking the network threads`, incomplete: false},
  {name: `replica.fetch.min.bytes`, type: `int`, defaultValue: `1`, importance: 'high', purpose: `Minimum bytes expected for each fetch response. If not enough bytes, wait up toreplica.fetch.wait.max.ms(broker config).`, incomplete: false},
  {name: `replica.fetch.wait.max.ms`, type: `int`, defaultValue: `500`, importance: 'high', purpose: `The maximum wait time for each fetcher request issued by follower replicas. This value should always be less than the replica.lag.time.max.ms at all times…`, incomplete: true},
  {name: `replica.high.watermark.checkpoint.interval.ms`, type: `long`, defaultValue: `5000 (5 seconds)`, importance: 'high', purpose: `The frequency with which the high watermark is saved out to disk`, incomplete: false},
  {name: `replica.lag.time.max.ms`, type: `long`, defaultValue: `30000 (30 seconds)`, importance: 'high', purpose: `If a follower hasn't sent any fetch requests or hasn't consumed up to the leader's log end offset for at least this time, the leader will remove the…`, incomplete: true},
  {name: `replica.socket.receive.buffer.bytes`, type: `int`, defaultValue: `65536 (64 kibibytes)`, importance: 'high', purpose: `The socket receive buffer for network requests to the leader for replicating data`, incomplete: false},
  {name: `replica.socket.timeout.ms`, type: `int`, defaultValue: `30000 (30 seconds)`, importance: 'high', purpose: `The socket timeout for network requests. Its value should be at least replica.fetch.wait.max.ms`, incomplete: false},
  {name: `request.timeout.ms`, type: `int`, defaultValue: `30000 (30 seconds)`, importance: 'high', purpose: `The configuration controls the maximum amount of time the client will wait for the response of a request. If the response is not received before the…`, incomplete: true},
  {name: `sasl.mechanism.controller.protocol`, type: `string`, defaultValue: `GSSAPI`, importance: 'high', purpose: `SASL mechanism used for communication with controllers. Default is GSSAPI.`, incomplete: false},
  {name: `share.coordinator.load.buffer.size`, type: `int`, defaultValue: `5242880`, importance: 'high', purpose: `Batch size for reading from the share-group state topic when loading state information into the cache (soft-limit, overridden if records are too large).`, incomplete: false},
  {name: `share.coordinator.state.topic.compression.codec`, type: `int`, defaultValue: `0`, importance: 'high', purpose: `Compression codec for the share-group state topic.`, incomplete: false},
  {name: `share.coordinator.state.topic.min.isr`, type: `short`, defaultValue: `2`, importance: 'high', purpose: `Overridden min.insync.replicas for the share-group state topic.`, incomplete: false},
  {name: `share.coordinator.state.topic.num.partitions`, type: `int`, defaultValue: `50`, importance: 'high', purpose: `The number of partitions for the share-group state topic (should not change after deployment).`, incomplete: false},
  {name: `share.coordinator.state.topic.replication.factor`, type: `short`, defaultValue: `3`, importance: 'high', purpose: `Replication factor for the share-group state topic. Topic creation will fail until the cluster size meets this replication factor requirement.`, incomplete: false},
  {name: `share.coordinator.state.topic.segment.bytes`, type: `int`, defaultValue: `104857600 (100 mebibytes)`, importance: 'high', purpose: `The log segment size for the share-group state topic.`, incomplete: false},
  {name: `share.coordinator.write.timeout.ms`, type: `int`, defaultValue: `5000 (5 seconds)`, importance: 'high', purpose: `The duration in milliseconds that the share coordinator will wait for all replicas of the share-group state topic to receive a write.`, incomplete: false},
  {name: `socket.receive.buffer.bytes`, type: `int`, defaultValue: `102400 (100 kibibytes)`, importance: 'high', purpose: `The SO_RCVBUF buffer of the socket server sockets. If the value is -1, the OS default will be used.`, incomplete: false},
  {name: `socket.request.max.bytes`, type: `int`, defaultValue: `104857600 (100 mebibytes)`, importance: 'high', purpose: `The maximum number of bytes in a socket request`, incomplete: false},
  {name: `socket.send.buffer.bytes`, type: `int`, defaultValue: `102400 (100 kibibytes)`, importance: 'high', purpose: `The SO_SNDBUF buffer of the socket server sockets. If the value is -1, the OS default will be used.`, incomplete: false},
  {name: `transaction.max.timeout.ms`, type: `int`, defaultValue: `900000 (15 minutes)`, importance: 'high', purpose: `The maximum allowed timeout for transactions. If a client’s requested transaction time exceed this, then the broker will return an error in…`, incomplete: true},
  {name: `transaction.state.log.load.buffer.size`, type: `int`, defaultValue: `5242880`, importance: 'high', purpose: `Batch size for reading from the transaction log segments when loading producer ids and transactions into the cache (soft-limit, overridden if records are…`, incomplete: true},
  {name: `transaction.state.log.min.isr`, type: `int`, defaultValue: `2`, importance: 'high', purpose: `The minimum number of replicas that must acknowledge a write to transaction topic in order to be considered successful.`, incomplete: false},
  {name: `transaction.state.log.num.partitions`, type: `int`, defaultValue: `50`, importance: 'high', purpose: `The number of partitions for the transaction topic (should not change after deployment).`, incomplete: false},
  {name: `transaction.state.log.replication.factor`, type: `short`, defaultValue: `3`, importance: 'high', purpose: `The replication factor for the transaction topic (set higher to ensure availability). Internal topic creation will fail until the cluster size meets this…`, incomplete: true},
  {name: `transaction.state.log.segment.bytes`, type: `int`, defaultValue: `104857600 (100 mebibytes)`, importance: 'high', purpose: `The transaction topic segment bytes should be kept relatively small in order to facilitate faster log compaction and cache loads`, incomplete: false},
  {name: `transactional.id.expiration.ms`, type: `int`, defaultValue: `604800000 (7 days)`, importance: 'high', purpose: `The time in ms that the transaction coordinator will wait without receiving any transaction status updates for the current transaction before expiring its…`, incomplete: true},
  {name: `unclean.leader.election.enable`, type: `boolean`, defaultValue: `false`, importance: 'high', purpose: `Indicates whether to enable replicas not in the ISR set to be elected as leader as a last resort, even though doing so may result in data loss Note: In…`, incomplete: true},
  {name: `broker.heartbeat.interval.ms`, type: `int`, defaultValue: `2000 (2 seconds)`, importance: 'medium', purpose: `The length of time in milliseconds between broker heartbeats.`, incomplete: false},
  {name: `broker.rack`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `Rack of the broker. This will be used in rack aware replication assignment for fault tolerance. Examples:RACK1,us-east-1d`, incomplete: false},
  {name: `broker.session.timeout.ms`, type: `int`, defaultValue: `9000 (9 seconds)`, importance: 'medium', purpose: `The length of time in milliseconds that a broker lease lasts if no heartbeats are made.`, incomplete: false},
  {name: `compression.gzip.level`, type: `int`, defaultValue: `-1`, importance: 'medium', purpose: `The compression level to use if compression.type is set to 'gzip'.`, incomplete: false},
  {name: `compression.lz4.level`, type: `int`, defaultValue: `9`, importance: 'medium', purpose: `The compression level to use if compression.type is set to 'lz4'.`, incomplete: false},
  {name: `compression.zstd.level`, type: `int`, defaultValue: `3`, importance: 'medium', purpose: `The compression level to use if compression.type is set to 'zstd'.`, incomplete: false},
  {name: `connections.max.idle.ms`, type: `long`, defaultValue: `600000 (10 minutes)`, importance: 'medium', purpose: `Idle connections timeout: the server socket processor threads close the connections that idle more than this`, incomplete: false},
  {name: `connections.max.reauth.ms`, type: `long`, defaultValue: `0`, importance: 'medium', purpose: `When explicitly set to a positive number (the default is 0, not a positive number), a session lifetime that will not exceed the configured value will be…`, incomplete: true},
  {name: `controlled.shutdown.enable`, type: `boolean`, defaultValue: `true`, importance: 'medium', purpose: `Enable controlled shutdown of the server.`, incomplete: false},
  {name: `controller.quorum.append.linger.ms`, type: `int`, defaultValue: `25`, importance: 'medium', purpose: `The duration in milliseconds that the leader will wait for writes to accumulate before flushing them to disk.`, incomplete: false},
  {name: `controller.quorum.request.timeout.ms`, type: `int`, defaultValue: `2000 (2 seconds)`, importance: 'medium', purpose: `The configuration controls the maximum amount of time the client will wait for the response of a request. If the response is not received before the…`, incomplete: true},
  {name: `controller.socket.timeout.ms`, type: `int`, defaultValue: `30000 (30 seconds)`, importance: 'medium', purpose: `The socket timeout for controller-to-broker channels.`, incomplete: false},
  {name: `default.replication.factor`, type: `int`, defaultValue: `1`, importance: 'medium', purpose: `The replication factor for automatically created topics, and for topics created with -1 as the replication factor`, incomplete: false},
  {name: `delegation.token.expiry.time.ms`, type: `long`, defaultValue: `86400000 (1 day)`, importance: 'medium', purpose: `The token validity time in milliseconds before the token needs to be renewed. Default value 1 day.`, incomplete: false},
  {name: `delegation.token.max.lifetime.ms`, type: `long`, defaultValue: `604800000 (7 days)`, importance: 'medium', purpose: `The token has a maximum lifetime beyond which it cannot be renewed anymore. Default value 7 days.`, incomplete: false},
  {name: `delegation.token.secret.key`, type: `password`, defaultValue: `null`, importance: 'medium', purpose: `Secret key to generate and verify delegation tokens. The same key must be configured across all the brokers. If using Kafka with KRaft, the key must also…`, incomplete: true},
  {name: `delete.records.purgatory.purge.interval.requests`, type: `int`, defaultValue: `1`, importance: 'medium', purpose: `The purge interval (in number of requests) of the delete records request purgatory`, incomplete: false},
  {name: `fetch.max.bytes`, type: `int`, defaultValue: `57671680 (55 mebibytes)`, importance: 'medium', purpose: `The maximum number of bytes we will return for a fetch request. Must be at least 1024.`, incomplete: false},
  {name: `fetch.purgatory.purge.interval.requests`, type: `int`, defaultValue: `1000`, importance: 'medium', purpose: `The purge interval (in number of requests) of the fetch request purgatory`, incomplete: false},
  {name: `group.consumer.assignors`, type: `list`, defaultValue: `uniform,range`, importance: 'medium', purpose: `The server side assignors as a list of either names for builtin assignors or full class names for customer assignors. The first one in the list is…`, incomplete: true},
  {name: `group.consumer.heartbeat.interval.ms`, type: `int`, defaultValue: `5000 (5 seconds)`, importance: 'medium', purpose: `The heartbeat interval given to the members of a consumer group.`, incomplete: false},
  {name: `group.consumer.max.heartbeat.interval.ms`, type: `int`, defaultValue: `15000 (15 seconds)`, importance: 'medium', purpose: `The maximum heartbeat interval for registered consumers.`, incomplete: false},
  {name: `group.consumer.max.session.timeout.ms`, type: `int`, defaultValue: `60000 (1 minute)`, importance: 'medium', purpose: `The maximum allowed session timeout for registered consumers.`, incomplete: false},
  {name: `group.consumer.max.size`, type: `int`, defaultValue: `2147483647`, importance: 'medium', purpose: `The maximum number of consumers that a single consumer group can accommodate. This value will only impact groups under the CONSUMER group protocol. To…`, incomplete: true},
  {name: `group.consumer.migration.policy`, type: `string`, defaultValue: `bidirectional`, importance: 'medium', purpose: `The config that enables converting the non-empty classic group using the consumer embedded protocol to the non-empty consumer group using the consumer…`, incomplete: true},
  {name: `group.consumer.min.heartbeat.interval.ms`, type: `int`, defaultValue: `5000 (5 seconds)`, importance: 'medium', purpose: `The minimum heartbeat interval for registered consumers.`, incomplete: false},
  {name: `group.consumer.min.session.timeout.ms`, type: `int`, defaultValue: `45000 (45 seconds)`, importance: 'medium', purpose: `The minimum allowed session timeout for registered consumers.`, incomplete: false},
  {name: `group.consumer.session.timeout.ms`, type: `int`, defaultValue: `45000 (45 seconds)`, importance: 'medium', purpose: `The timeout to detect client failures when using the consumer group protocol.`, incomplete: false},
  {name: `group.coordinator.append.linger.ms`, type: `int`, defaultValue: `5`, importance: 'medium', purpose: `The duration in milliseconds that the coordinator will wait for writes to accumulate before flushing them to disk. Transactional writes are not accumulated.`, incomplete: false},
  {name: `group.coordinator.rebalance.protocols`, type: `list`, defaultValue: `classic,consumer`, importance: 'medium', purpose: `The list of enabled rebalance protocols.The share rebalance protocol is in early access and therefore must not be used in production.`, incomplete: false},
  {name: `group.initial.rebalance.delay.ms`, type: `int`, defaultValue: `3000 (3 seconds)`, importance: 'medium', purpose: `The amount of time the group coordinator will wait for more consumers to join a new group before performing the first rebalance. A longer delay means…`, incomplete: true},
  {name: `group.max.session.timeout.ms`, type: `int`, defaultValue: `1800000 (30 minutes)`, importance: 'medium', purpose: `The maximum allowed session timeout for registered consumers. Longer timeouts give consumers more time to process messages in between heartbeats at the…`, incomplete: true},
  {name: `group.max.size`, type: `int`, defaultValue: `2147483647`, importance: 'medium', purpose: `The maximum number of consumers that a single consumer group can accommodate.`, incomplete: false},
  {name: `group.min.session.timeout.ms`, type: `int`, defaultValue: `6000 (6 seconds)`, importance: 'medium', purpose: `The minimum allowed session timeout for registered consumers. Shorter timeouts result in quicker failure detection at the cost of more frequent consumer…`, incomplete: true},
  {name: `group.share.delivery.count.limit`, type: `int`, defaultValue: `5`, importance: 'medium', purpose: `The maximum number of delivery attempts for a record delivered to a share group.`, incomplete: false},
  {name: `group.share.heartbeat.interval.ms`, type: `int`, defaultValue: `5000 (5 seconds)`, importance: 'medium', purpose: `The heartbeat interval given to the members of a share group.`, incomplete: false},
  {name: `group.share.max.groups`, type: `short`, defaultValue: `10`, importance: 'medium', purpose: `The maximum number of share groups.`, incomplete: false},
  {name: `group.share.max.heartbeat.interval.ms`, type: `int`, defaultValue: `15000 (15 seconds)`, importance: 'medium', purpose: `The maximum heartbeat interval for share group members.`, incomplete: false},
  {name: `group.share.max.record.lock.duration.ms`, type: `int`, defaultValue: `60000 (1 minute)`, importance: 'medium', purpose: `The record acquisition lock maximum duration in milliseconds for share groups.`, incomplete: false},
  {name: `group.share.max.session.timeout.ms`, type: `int`, defaultValue: `60000 (1 minute)`, importance: 'medium', purpose: `The maximum allowed session timeout for share group members.`, incomplete: false},
  {name: `group.share.max.size`, type: `int`, defaultValue: `200`, importance: 'medium', purpose: `The maximum number of members that a single share group can accommodate.`, incomplete: false},
  {name: `group.share.min.heartbeat.interval.ms`, type: `int`, defaultValue: `5000 (5 seconds)`, importance: 'medium', purpose: `The minimum heartbeat interval for share group members.`, incomplete: false},
  {name: `group.share.min.record.lock.duration.ms`, type: `int`, defaultValue: `15000 (15 seconds)`, importance: 'medium', purpose: `The record acquisition lock minimum duration in milliseconds for share groups.`, incomplete: false},
  {name: `group.share.min.session.timeout.ms`, type: `int`, defaultValue: `45000 (45 seconds)`, importance: 'medium', purpose: `The minimum allowed session timeout for share group members.`, incomplete: false},
  {name: `group.share.partition.max.record.locks`, type: `int`, defaultValue: `200`, importance: 'medium', purpose: `Share-group record lock limit per share-partition.`, incomplete: false},
  {name: `group.share.record.lock.duration.ms`, type: `int`, defaultValue: `30000 (30 seconds)`, importance: 'medium', purpose: `The record acquisition lock duration in milliseconds for share groups.`, incomplete: false},
  {name: `group.share.session.timeout.ms`, type: `int`, defaultValue: `45000 (45 seconds)`, importance: 'medium', purpose: `The timeout to detect client failures when using the share group protocol.`, incomplete: false},
  {name: `initial.broker.registration.timeout.ms`, type: `int`, defaultValue: `60000 (1 minute)`, importance: 'medium', purpose: `When initially registering with the controller quorum, the number of milliseconds to wait before declaring failure and exiting the broker process.`, incomplete: false},
  {name: `inter.broker.listener.name`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `Name of listener used for communication between brokers. If this is unset, the listener name is defined by security.inter.broker.protocol. It is an error…`, incomplete: true},
  {name: `log.cleaner.backoff.ms`, type: `long`, defaultValue: `15000 (15 seconds)`, importance: 'medium', purpose: `The amount of time to sleep when there are no logs to clean`, incomplete: false},
  {name: `log.cleaner.dedupe.buffer.size`, type: `long`, defaultValue: `134217728`, importance: 'medium', purpose: `The total memory used for log deduplication across all cleaner threads`, incomplete: false},
  {name: `log.cleaner.delete.retention.ms`, type: `long`, defaultValue: `86400000 (1 day)`, importance: 'medium', purpose: `The amount of time to retain tombstone message markers for log compacted topics. This setting also gives a bound on the time in which a consumer must…`, incomplete: true},
  {name: `log.cleaner.enable`, type: `boolean`, defaultValue: `true`, importance: 'medium', purpose: `Enable the log cleaner process to run on the server. Should be enabled if using any topics with a cleanup.policy=compact including the internal offsets…`, incomplete: true},
  {name: `log.cleaner.io.buffer.load.factor`, type: `double`, defaultValue: `0.9`, importance: 'medium', purpose: `Log cleaner dedupe buffer load factor. The percentage full the dedupe buffer can become. A higher value will allow more log to be cleaned at once but will…`, incomplete: true},
  {name: `log.cleaner.io.buffer.size`, type: `int`, defaultValue: `524288`, importance: 'medium', purpose: `The total memory used for log cleaner I/O buffers across all cleaner threads`, incomplete: false},
  {name: `log.cleaner.io.max.bytes.per.second`, type: `double`, defaultValue: `1.7976931348623157E308`, importance: 'medium', purpose: `The log cleaner will be throttled so that the sum of its read and write i/o will be less than this value on average`, incomplete: false},
  {name: `log.cleaner.max.compaction.lag.ms`, type: `long`, defaultValue: `9223372036854775807`, importance: 'medium', purpose: `The maximum time a message will remain ineligible for compaction in the log. Only applicable for logs that are being compacted.`, incomplete: false},
  {name: `log.cleaner.min.cleanable.ratio`, type: `double`, defaultValue: `0.5`, importance: 'medium', purpose: `The minimum ratio of dirty log to total log for a log to eligible for cleaning. If the log.cleaner.max.compaction.lag.ms or the…`, incomplete: true},
  {name: `log.cleaner.min.compaction.lag.ms`, type: `long`, defaultValue: `0`, importance: 'medium', purpose: `The minimum time a message will remain uncompacted in the log. Only applicable for logs that are being compacted.`, incomplete: false},
  {name: `log.cleaner.threads`, type: `int`, defaultValue: `1`, importance: 'medium', purpose: `The number of background threads to use for log cleaning`, incomplete: false},
  {name: `log.cleanup.policy`, type: `list`, defaultValue: `delete`, importance: 'medium', purpose: `The default cleanup policy for segments beyond the retention window. A comma separated list of valid policies.`, incomplete: false},
  {name: `log.index.interval.bytes`, type: `int`, defaultValue: `4096 (4 kibibytes)`, importance: 'medium', purpose: `The interval with which we add an entry to the offset index.`, incomplete: false},
  {name: `log.index.size.max.bytes`, type: `int`, defaultValue: `10485760 (10 mebibytes)`, importance: 'medium', purpose: `The maximum size in bytes of the offset index`, incomplete: false},
  {name: `log.local.retention.bytes`, type: `long`, defaultValue: `-2`, importance: 'medium', purpose: `The maximum size of local log segments that can grow for a partition before it gets eligible for deletion. Default value is -2, it represents…`, incomplete: true},
  {name: `log.local.retention.ms`, type: `long`, defaultValue: `-2`, importance: 'medium', purpose: `The number of milliseconds to keep the local log segments before it gets eligible for deletion. Default value is -2, it represents log.retention.ms value…`, incomplete: true},
  {name: `log.message.timestamp.after.max.ms`, type: `long`, defaultValue: `3600000 (1 hour)`, importance: 'medium', purpose: `This configuration sets the allowable timestamp difference between the message timestamp and the broker's timestamp. The message timestamp can be later…`, incomplete: true},
  {name: `log.message.timestamp.before.max.ms`, type: `long`, defaultValue: `9223372036854775807`, importance: 'medium', purpose: `This configuration sets the allowable timestamp difference between the broker's timestamp and the message timestamp. The message timestamp can be earlier…`, incomplete: true},
  {name: `log.message.timestamp.type`, type: `string`, defaultValue: `CreateTime`, importance: 'medium', purpose: `Define whether the timestamp in the message is message create time or log append time. The value should be eitherCreateTime orLogAppendTime.`, incomplete: false},
  {name: `log.preallocate`, type: `boolean`, defaultValue: `false`, importance: 'medium', purpose: `Should pre allocate file when create new segment? If you are using Kafka on Windows, you probably need to set it to true.`, incomplete: false},
  {name: `log.retention.check.interval.ms`, type: `long`, defaultValue: `300000 (5 minutes)`, importance: 'medium', purpose: `The frequency in milliseconds that the log cleaner checks whether any log is eligible for deletion`, incomplete: false},
  {name: `max.connection.creation.rate`, type: `int`, defaultValue: `2147483647`, importance: 'medium', purpose: `The maximum connection creation rate we allow in the broker at any time. Listener-level limits may also be configured by prefixing the config name with the…`, incomplete: true},
  {name: `max.connections`, type: `int`, defaultValue: `2147483647`, importance: 'medium', purpose: `The maximum number of connections we allow in the broker at any time. This limit is applied in addition to any per-ip limits configured using…`, incomplete: true},
  {name: `max.connections.per.ip`, type: `int`, defaultValue: `2147483647`, importance: 'medium', purpose: `The maximum number of connections we allow from each ip address. This can be set to 0 if there are overrides configured using…`, incomplete: true},
  {name: `max.connections.per.ip.overrides`, type: `string`, defaultValue: `""`, importance: 'medium', purpose: `A comma-separated list of per-ip or hostname overrides to the default maximum number of connections. An example value is "hostName:100,127.0.0.1:200"`, incomplete: false},
  {name: `max.incremental.fetch.session.cache.slots`, type: `int`, defaultValue: `1000`, importance: 'medium', purpose: `The maximum number of total incremental fetch sessions that we will maintain. FetchSessionCache is sharded into 8 shards and the limit is equally divided…`, incomplete: true},
  {name: `max.request.partition.size.limit`, type: `int`, defaultValue: `2000`, importance: 'medium', purpose: `The maximum number of partitions can be served in one request.`, incomplete: false},
  {name: `num.partitions`, type: `int`, defaultValue: `1`, importance: 'medium', purpose: `The default number of log partitions per topic`, incomplete: false},
  {name: `principal.builder.class`, type: `class`, defaultValue: `org.apache.kafka.common.security.authenticator.DefaultKafkaPrincipalBuilder`, importance: 'medium', purpose: `The fully qualified name of a class that implements the KafkaPrincipalBuilder interface, which is used to build the KafkaPrincipal object used during…`, incomplete: true},
  {name: `producer.purgatory.purge.interval.requests`, type: `int`, defaultValue: `1000`, importance: 'medium', purpose: `The purge interval (in number of requests) of the producer request purgatory`, incomplete: false},
  {name: `queued.max.request.bytes`, type: `long`, defaultValue: `-1`, importance: 'medium', purpose: `The number of queued bytes allowed before no more requests are read`, incomplete: false},
  {name: `remote.fetch.max.wait.ms`, type: `int`, defaultValue: `500`, importance: 'medium', purpose: `The maximum amount of time the server will wait before answering the remote fetch request`, incomplete: false},
  {name: `remote.list.offsets.request.timeout.ms`, type: `long`, defaultValue: `30000 (30 seconds)`, importance: 'medium', purpose: `The maximum amount of time the server will wait for the remote list offsets request to complete.`, incomplete: false},
  {name: `remote.log.manager.copier.thread.pool.size`, type: `int`, defaultValue: `10`, importance: 'medium', purpose: `Size of the thread pool used in scheduling tasks to copy segments.`, incomplete: false},
  {name: `remote.log.manager.copy.max.bytes.per.second`, type: `long`, defaultValue: `9223372036854775807`, importance: 'medium', purpose: `The maximum number of bytes that can be copied from local storage to remote storage per second. This is a global limit for all the partitions that are…`, incomplete: true},
  {name: `remote.log.manager.copy.quota.window.num`, type: `int`, defaultValue: `11`, importance: 'medium', purpose: `The number of samples to retain in memory for remote copy quota management. The default value is 11, which means there are 10 whole windows + 1 current window.`, incomplete: false},
  {name: `remote.log.manager.copy.quota.window.size.seconds`, type: `int`, defaultValue: `1`, importance: 'medium', purpose: `The time span of each sample for remote copy quota management. The default value is 1 second.`, incomplete: false},
  {name: `remote.log.manager.expiration.thread.pool.size`, type: `int`, defaultValue: `10`, importance: 'medium', purpose: `Size of the thread pool used in scheduling tasks to clean up the expired remote log segments.`, incomplete: false},
  {name: `remote.log.manager.fetch.max.bytes.per.second`, type: `long`, defaultValue: `9223372036854775807`, importance: 'medium', purpose: `The maximum number of bytes that can be fetched from remote storage to local storage per second. This is a global limit for all the partitions that are…`, incomplete: true},
  {name: `remote.log.manager.fetch.quota.window.num`, type: `int`, defaultValue: `11`, importance: 'medium', purpose: `The number of samples to retain in memory for remote fetch quota management. The default value is 11, which means there are 10 whole windows + 1 current window.`, incomplete: false},
  {name: `remote.log.manager.fetch.quota.window.size.seconds`, type: `int`, defaultValue: `1`, importance: 'medium', purpose: `The time span of each sample for remote fetch quota management. The default value is 1 second.`, incomplete: false},
  {name: `remote.log.manager.thread.pool.size`, type: `int`, defaultValue: `2`, importance: 'medium', purpose: `Size of the thread pool used in scheduling follower tasks to read the highest-uploaded remote-offset for follower partitions.`, incomplete: false},
  {name: `remote.log.metadata.manager.class.name`, type: `string`, defaultValue: `org.apache.kafka.server.log.remote.metadata.storage.TopicBasedRemoteLogMetadataManager`, importance: 'medium', purpose: `Fully qualified class name of RemoteLogMetadataManager implementation.`, incomplete: false},
  {name: `remote.log.metadata.manager.class.path`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `Class path of the RemoteLogMetadataManager implementation. If specified, the RemoteLogMetadataManager implementation and its dependent libraries will be…`, incomplete: true},
  {name: `remote.log.metadata.manager.impl.prefix`, type: `string`, defaultValue: `rlmm.config.`, importance: 'medium', purpose: `Prefix used for properties to be passed to RemoteLogMetadataManager implementation. For example this value can be rlmm.config..`, incomplete: false},
  {name: `remote.log.metadata.manager.listener.name`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `Listener name of the local broker to which it should get connected if needed by RemoteLogMetadataManager implementation.`, incomplete: false},
  {name: `remote.log.reader.max.pending.tasks`, type: `int`, defaultValue: `100`, importance: 'medium', purpose: `Maximum remote log reader thread pool task queue size. If the task queue is full, fetch requests are served with an error.`, incomplete: false},
  {name: `remote.log.reader.threads`, type: `int`, defaultValue: `10`, importance: 'medium', purpose: `Size of the thread pool that is allocated for handling remote log reads.`, incomplete: false},
  {name: `remote.log.storage.manager.class.name`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `Fully qualified class name of RemoteStorageManager implementation.`, incomplete: false},
  {name: `remote.log.storage.manager.class.path`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `Class path of the RemoteStorageManager implementation. If specified, the RemoteStorageManager implementation and its dependent libraries will be loaded by…`, incomplete: true},
  {name: `remote.log.storage.manager.impl.prefix`, type: `string`, defaultValue: `rsm.config.`, importance: 'medium', purpose: `Prefix used for properties to be passed to RemoteStorageManager implementation. For example this value can be rsm.config..`, incomplete: false},
  {name: `remote.log.storage.system.enable`, type: `boolean`, defaultValue: `false`, importance: 'medium', purpose: `Whether to enable tiered storage functionality in a broker or not. When it is true broker starts all the services required for the tiered storage functionality.`, incomplete: false},
  {name: `replica.fetch.backoff.ms`, type: `int`, defaultValue: `1000 (1 second)`, importance: 'medium', purpose: `The amount of time to sleep when fetch partition error occurs.`, incomplete: false},
  {name: `replica.fetch.max.bytes`, type: `int`, defaultValue: `1048576 (1 mebibyte)`, importance: 'medium', purpose: `The number of bytes of messages to attempt to fetch for each partition. This is not an absolute maximum, if the first record batch in the first non-empty…`, incomplete: true},
  {name: `replica.fetch.response.max.bytes`, type: `int`, defaultValue: `10485760 (10 mebibytes)`, importance: 'medium', purpose: `Maximum bytes expected for the entire fetch response. Records are fetched in batches, and if the first record batch in the first non-empty partition of the…`, incomplete: true},
  {name: `replica.selector.class`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `The fully qualified class name that implements ReplicaSelector. This is used by the broker to find the preferred read replica. By default, we use an…`, incomplete: true},
  {name: `sasl.client.callback.handler.class`, type: `class`, defaultValue: `null`, importance: 'medium', purpose: `The fully qualified name of a SASL client callback handler class that implements the AuthenticateCallbackHandler interface.`, incomplete: false},
  {name: `sasl.enabled.mechanisms`, type: `list`, defaultValue: `GSSAPI`, importance: 'medium', purpose: `The list of SASL mechanisms enabled in the Kafka server. The list may contain any mechanism for which a security provider is available. Only GSSAPI is…`, incomplete: true},
  {name: `sasl.jaas.config`, type: `password`, defaultValue: `null`, importance: 'medium', purpose: `JAAS login context parameters for SASL connections in the format used by JAAS configuration files. JAAS configuration file format is described here. The…`, incomplete: true},
  {name: `sasl.kerberos.kinit.cmd`, type: `string`, defaultValue: `/usr/bin/kinit`, importance: 'medium', purpose: `Kerberos kinit command path.`, incomplete: false},
  {name: `sasl.kerberos.min.time.before.relogin`, type: `long`, defaultValue: `60000`, importance: 'medium', purpose: `Login thread sleep time between refresh attempts.`, incomplete: false},
  {name: `sasl.kerberos.principal.to.local.rules`, type: `list`, defaultValue: `DEFAULT`, importance: 'medium', purpose: `A list of rules for mapping from principal names to short names (typically operating system usernames). The rules are evaluated in order and the first rule…`, incomplete: true},
  {name: `sasl.kerberos.service.name`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `The Kerberos principal name that Kafka runs as. This can be defined either in Kafka's JAAS config or in Kafka's config.`, incomplete: false},
  {name: `sasl.kerberos.ticket.renew.jitter`, type: `double`, defaultValue: `0.05`, importance: 'medium', purpose: `Percentage of random jitter added to the renewal time.`, incomplete: false},
  {name: `sasl.kerberos.ticket.renew.window.factor`, type: `double`, defaultValue: `0.8`, importance: 'medium', purpose: `Login thread will sleep until the specified window factor of time from last refresh to ticket's expiry has been reached, at which time it will try to renew…`, incomplete: true},
  {name: `sasl.login.callback.handler.class`, type: `class`, defaultValue: `null`, importance: 'medium', purpose: `The fully qualified name of a SASL login callback handler class that implements the AuthenticateCallbackHandler interface. For brokers, login callback…`, incomplete: true},
  {name: `sasl.login.class`, type: `class`, defaultValue: `null`, importance: 'medium', purpose: `The fully qualified name of a class that implements the Login interface. For brokers, login config must be prefixed with listener prefix and SASL mechanism…`, incomplete: true},
  {name: `sasl.login.refresh.buffer.seconds`, type: `short`, defaultValue: `300`, importance: 'medium', purpose: `The amount of buffer time before credential expiration to maintain when refreshing a credential, in seconds. If a refresh would otherwise occur closer to…`, incomplete: true},
  {name: `sasl.login.refresh.min.period.seconds`, type: `short`, defaultValue: `60`, importance: 'medium', purpose: `The desired minimum time for the login refresh thread to wait before refreshing a credential, in seconds. Legal values are between 0 and 900 (15 minutes);…`, incomplete: true},
  {name: `sasl.login.refresh.window.factor`, type: `double`, defaultValue: `0.8`, importance: 'medium', purpose: `Login refresh thread will sleep until the specified window factor relative to the credential's lifetime has been reached, at which time it will try to…`, incomplete: true},
  {name: `sasl.login.refresh.window.jitter`, type: `double`, defaultValue: `0.05`, importance: 'medium', purpose: `The maximum amount of random jitter relative to the credential's lifetime that is added to the login refresh thread's sleep time. Legal values are between…`, incomplete: true},
  {name: `sasl.mechanism.inter.broker.protocol`, type: `string`, defaultValue: `GSSAPI`, importance: 'medium', purpose: `SASL mechanism used for inter-broker communication. Default is GSSAPI.`, incomplete: false},
  {name: `sasl.oauthbearer.jwks.endpoint.url`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `The OAuth/OIDC provider URL from which the provider's JWKS (JSON Web Key Set) can be retrieved. The URL can be HTTP(S)-based or file-based. If the URL is…`, incomplete: true},
  {name: `sasl.oauthbearer.token.endpoint.url`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `The URL for the OAuth/OIDC identity provider. If the URL is HTTP(S)-based, it is the issuer's token endpoint URL to which requests will be made to login…`, incomplete: true},
  {name: `sasl.server.callback.handler.class`, type: `class`, defaultValue: `null`, importance: 'medium', purpose: `The fully qualified name of a SASL server callback handler class that implements the AuthenticateCallbackHandler interface. Server callback handlers must…`, incomplete: true},
  {name: `sasl.server.max.receive.size`, type: `int`, defaultValue: `524288`, importance: 'medium', purpose: `The maximum receive size allowed before and during initial SASL authentication. Default receive size is 512KB. GSSAPI limits requests to 64K, but we allow…`, incomplete: true},
  {name: `security.inter.broker.protocol`, type: `string`, defaultValue: `PLAINTEXT`, importance: 'medium', purpose: `Security protocol used to communicate between brokers. It is an error to set this and inter.broker.listener.name properties at the same time.`, incomplete: false},
  {name: `share.coordinator.append.linger.ms`, type: `int`, defaultValue: `10`, importance: 'medium', purpose: `The duration in milliseconds that the share coordinator will wait for writes to accumulate before flushing them to disk.`, incomplete: false},
  {name: `share.coordinator.snapshot.update.records.per.snapshot`, type: `int`, defaultValue: `500`, importance: 'medium', purpose: `The number of update records the share coordinator writes between snapshot records.`, incomplete: false},
  {name: `share.coordinator.threads`, type: `int`, defaultValue: `1`, importance: 'medium', purpose: `The number of threads used by the share coordinator.`, incomplete: false},
  {name: `share.fetch.purgatory.purge.interval.requests`, type: `int`, defaultValue: `1000`, importance: 'medium', purpose: `The purge interval (in number of requests) of the share fetch request purgatory`, incomplete: false},
  {name: `socket.connection.setup.timeout.max.ms`, type: `long`, defaultValue: `30000 (30 seconds)`, importance: 'medium', purpose: `The maximum amount of time the client will wait for the socket connection to be established. The connection setup timeout will increase exponentially for…`, incomplete: true},
  {name: `socket.connection.setup.timeout.ms`, type: `long`, defaultValue: `10000 (10 seconds)`, importance: 'medium', purpose: `The amount of time the client will wait for the socket connection to be established. If the connection is not built before the timeout elapses, clients…`, incomplete: true},
  {name: `socket.listen.backlog.size`, type: `int`, defaultValue: `50`, importance: 'medium', purpose: `The maximum number of pending connections on the socket. In Linux, you may also need to configuresomaxconn andtcp_max_syn_backlog kernel parameters…`, incomplete: true},
  {name: `ssl.cipher.suites`, type: `list`, defaultValue: `""`, importance: 'medium', purpose: `A list of cipher suites. This is a named combination of authentication, encryption, MAC and key exchange algorithm used to negotiate the security settings…`, incomplete: true},
  {name: `ssl.client.auth`, type: `string`, defaultValue: `none`, importance: 'medium', purpose: `Configures kafka broker to request client authentication. The following settings are common:`, incomplete: false},
  {name: `ssl.enabled.protocols`, type: `list`, defaultValue: `TLSv1.2,TLSv1.3`, importance: 'medium', purpose: `The list of protocols enabled for SSL connections. The default is 'TLSv1.2,TLSv1.3'. This means that clients and servers will prefer TLSv1.3 if both…`, incomplete: true},
  {name: `ssl.key.password`, type: `password`, defaultValue: `null`, importance: 'medium', purpose: `The password of the private key in the key store file or the PEM key specified in 'ssl.keystore.key'.`, incomplete: false},
  {name: `ssl.keymanager.algorithm`, type: `string`, defaultValue: `SunX509`, importance: 'medium', purpose: `The algorithm used by key manager factory for SSL connections. Default value is the key manager factory algorithm configured for the Java Virtual Machine.`, incomplete: false},
  {name: `ssl.keystore.certificate.chain`, type: `password`, defaultValue: `null`, importance: 'medium', purpose: `Certificate chain in the format specified by 'ssl.keystore.type'. Default SSL engine factory supports only PEM format with a list of X.509 certificates`, incomplete: false},
  {name: `ssl.keystore.key`, type: `password`, defaultValue: `null`, importance: 'medium', purpose: `Private key in the format specified by 'ssl.keystore.type'. Default SSL engine factory supports only PEM format with PKCS#8 keys. If the key is encrypted,…`, incomplete: true},
  {name: `ssl.keystore.location`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `The location of the key store file. This is optional for client and can be used for two-way authentication for client.`, incomplete: false},
  {name: `ssl.keystore.password`, type: `password`, defaultValue: `null`, importance: 'medium', purpose: `The store password for the key store file. This is optional for client and only needed if 'ssl.keystore.location' is configured. Key store password is not…`, incomplete: true},
  {name: `ssl.keystore.type`, type: `string`, defaultValue: `JKS`, importance: 'medium', purpose: `The file format of the key store file. This is optional for client. The values currently supported by the default ssl.engine.factory.class are [JKS,…`, incomplete: true},
  {name: `ssl.protocol`, type: `string`, defaultValue: `TLSv1.3`, importance: 'medium', purpose: `The SSL protocol used to generate the SSLContext. The default is 'TLSv1.3', which should be fine for most use cases. A typical alternative to the default…`, incomplete: true},
  {name: `ssl.provider`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `The name of the security provider used for SSL connections. Default value is the default security provider of the JVM.`, incomplete: false},
  {name: `ssl.trustmanager.algorithm`, type: `string`, defaultValue: `PKIX`, importance: 'medium', purpose: `The algorithm used by trust manager factory for SSL connections. Default value is the trust manager factory algorithm configured for the Java Virtual Machine.`, incomplete: false},
  {name: `ssl.truststore.certificates`, type: `password`, defaultValue: `null`, importance: 'medium', purpose: `Trusted certificates in the format specified by 'ssl.truststore.type'. Default SSL engine factory supports only PEM format with X.509 certificates.`, incomplete: false},
  {name: `ssl.truststore.location`, type: `string`, defaultValue: `null`, importance: 'medium', purpose: `The location of the trust store file.`, incomplete: false},
  {name: `ssl.truststore.password`, type: `password`, defaultValue: `null`, importance: 'medium', purpose: `The password for the trust store file. If a password is not set, trust store file configured will still be used, but integrity checking is disabled. Trust…`, incomplete: true},
  {name: `ssl.truststore.type`, type: `string`, defaultValue: `JKS`, importance: 'medium', purpose: `The file format of the trust store file. The values currently supported by the default ssl.engine.factory.class are [JKS, PKCS12, PEM].`, incomplete: false},
  {name: `alter.config.policy.class.name`, type: `class`, defaultValue: `null`, importance: 'low', purpose: `The alter configs policy class that should be used for validation. The class should implement the \`org.apache.kafka.server.policy.AlterConfigPolicy interface.`, incomplete: true},
  {name: `alter.log.dirs.replication.quota.window.num`, type: `int`, defaultValue: `11`, importance: 'low', purpose: `The number of samples to retain in memory for alter log dirs replication quotas`, incomplete: false},
  {name: `alter.log.dirs.replication.quota.window.size.seconds`, type: `int`, defaultValue: `1`, importance: 'low', purpose: `The time span of each sample for alter log dirs replication quotas`, incomplete: false},
  {name: `authorizer.class.name`, type: `string`, defaultValue: `""`, importance: 'low', purpose: `The fully qualified name of a class that implements the \`org.apache.kafka.server.authorizer.Authorizer\` interface, which is used by the broker for authorization.`, incomplete: false},
  {name: `client.quota.callback.class`, type: `class`, defaultValue: `null`, importance: 'low', purpose: `The fully qualified name of a class that implements the ClientQuotaCallback interface, which is used to determine quota limits applied to client requests.…`, incomplete: true},
  {name: `connection.failed.authentication.delay.ms`, type: `int`, defaultValue: `100`, importance: 'low', purpose: `Connection close delay on failed authentication: this is the time (in milliseconds) by which connection close will be delayed on authentication failure.…`, incomplete: true},
  {name: `controller.quorum.retry.backoff.ms`, type: `int`, defaultValue: `20`, importance: 'low', purpose: `The amount of time to wait before attempting to retry a failed request to a given topic partition. This avoids repeatedly sending requests in a tight loop…`, incomplete: true},
  {name: `controller.quota.window.num`, type: `int`, defaultValue: `11`, importance: 'low', purpose: `The number of samples to retain in memory for controller mutation quotas`, incomplete: false},
  {name: `controller.quota.window.size.seconds`, type: `int`, defaultValue: `1`, importance: 'low', purpose: `The time span of each sample for controller mutations quotas`, incomplete: false},
  {name: `create.topic.policy.class.name`, type: `class`, defaultValue: `null`, importance: 'low', purpose: `The create topic policy class that should be used for validation. The class should implement the \`org.apache.kafka.server.policy.CreateTopicPolicy interface.…`, incomplete: true},
  {name: `delegation.token.expiry.check.interval.ms`, type: `long`, defaultValue: `3600000 (1 hour)`, importance: 'low', purpose: `Scan interval to remove expired delegation tokens.`, incomplete: false},
  {name: `kafka.metrics.polling.interval.secs`, type: `int`, defaultValue: `10`, importance: 'low', purpose: `The metrics polling interval (in seconds) which can be used in kafka.metrics.reporters implementations.`, incomplete: false},
  {name: `kafka.metrics.reporters`, type: `list`, defaultValue: `""`, importance: 'low', purpose: `A list of classes to use as Yammer metrics custom reporters. The reporters should implementkafka.metrics.KafkaMetricsReporter trait. If a client wants to…`, incomplete: true},
  {name: `listener.security.protocol.map`, type: `string`, defaultValue: `SASL_SSL:SASL_SSL,PLAINTEXT:PLAINTEXT,SSL:SSL,SASL_PLAINTEXT:SASL_PLAINTEXT`, importance: 'low', purpose: `Map between listener names and security protocols. This must be defined for the same security protocol to be usable in more than one port or IP. For…`, incomplete: true},
  {name: `log.dir.failure.timeout.ms`, type: `long`, defaultValue: `30000 (30 seconds)`, importance: 'low', purpose: `If the broker is unable to successfully communicate to the controller that some log directory has failed for longer than this time, the broker will fail…`, incomplete: true},
  {name: `metadata.max.idle.interval.ms`, type: `int`, defaultValue: `500`, importance: 'low', purpose: `This configuration controls how often the active controller should write no-op records to the metadata partition. If the value is 0, no-op records are not…`, incomplete: true},
  {name: `metric.reporters`, type: `list`, defaultValue: `org.apache.kafka.common.metrics.JmxReporter`, importance: 'low', purpose: `A list of classes to use as metrics reporters. Implementing the \`org.apache.kafka.common.metrics.MetricsReporter interface allows plugging in classes that…`, incomplete: true},
  {name: `metrics.num.samples`, type: `int`, defaultValue: `2`, importance: 'low', purpose: `The number of samples maintained to compute metrics.`, incomplete: false},
  {name: `metrics.recording.level`, type: `string`, defaultValue: `INFO`, importance: 'low', purpose: `The highest recording level for metrics. It has three levels for recording metrics - info, debug, and trace.INFO level records only essential metrics…`, incomplete: true},
  {name: `metrics.sample.window.ms`, type: `long`, defaultValue: `30000 (30 seconds)`, importance: 'low', purpose: `The window of time a metrics sample is computed over.`, incomplete: false},
  {name: `producer.id.expiration.ms`, type: `int`, defaultValue: `86400000 (1 day)`, importance: 'low', purpose: `The time in ms that a topic partition leader will wait before expiring producer IDs. Producer IDs will not expire while a transaction associated to them is…`, incomplete: true},
  {name: `quota.window.num`, type: `int`, defaultValue: `11`, importance: 'low', purpose: `The number of samples to retain in memory for client quotas`, incomplete: false},
  {name: `quota.window.size.seconds`, type: `int`, defaultValue: `1`, importance: 'low', purpose: `The time span of each sample for client quotas`, incomplete: false},
  {name: `remote.log.index.file.cache.total.size.bytes`, type: `long`, defaultValue: `1073741824 (1 gibibyte)`, importance: 'low', purpose: `The total size of the space allocated to store index files fetched from remote storage in the local storage.`, incomplete: false},
  {name: `remote.log.manager.task.interval.ms`, type: `long`, defaultValue: `30000 (30 seconds)`, importance: 'low', purpose: `Interval at which remote log manager runs the scheduled tasks like copy segments, and clean up remote log segments.`, incomplete: false},
  {name: `remote.log.metadata.custom.metadata.max.bytes`, type: `int`, defaultValue: `128`, importance: 'low', purpose: `The maximum size of custom metadata in bytes that the broker should accept from a remote storage plugin. If custom metadata exceeds this limit, the updated…`, incomplete: true},
  {name: `replication.quota.window.num`, type: `int`, defaultValue: `11`, importance: 'low', purpose: `The number of samples to retain in memory for replication quotas`, incomplete: false},
  {name: `replication.quota.window.size.seconds`, type: `int`, defaultValue: `1`, importance: 'low', purpose: `The time span of each sample for replication quotas`, incomplete: false},
  {name: `sasl.login.connect.timeout.ms`, type: `int`, defaultValue: `null`, importance: 'low', purpose: `The (optional) value in milliseconds for the external authentication provider connection timeout. Currently applies only to OAUTHBEARER.`, incomplete: false},
  {name: `sasl.login.read.timeout.ms`, type: `int`, defaultValue: `null`, importance: 'low', purpose: `The (optional) value in milliseconds for the external authentication provider read timeout. Currently applies only to OAUTHBEARER.`, incomplete: false},
  {name: `sasl.login.retry.backoff.max.ms`, type: `long`, defaultValue: `10000 (10 seconds)`, importance: 'low', purpose: `The (optional) value in milliseconds for the maximum wait between login attempts to the external authentication provider. Login uses an exponential backoff…`, incomplete: true},
  {name: `sasl.login.retry.backoff.ms`, type: `long`, defaultValue: `100`, importance: 'low', purpose: `The (optional) value in milliseconds for the initial wait between login attempts to the external authentication provider. Login uses an exponential backoff…`, incomplete: true},
  {name: `sasl.oauthbearer.clock.skew.seconds`, type: `int`, defaultValue: `30`, importance: 'low', purpose: `The (optional) value in seconds to allow for differences between the time of the OAuth/OIDC identity provider and the broker.`, incomplete: false},
  {name: `sasl.oauthbearer.expected.audience`, type: `list`, defaultValue: `null`, importance: 'low', purpose: `The (optional) comma-delimited setting for the broker to use to verify that the JWT was issued for one of the expected audiences. The JWT will be inspected…`, incomplete: true},
  {name: `sasl.oauthbearer.expected.issuer`, type: `string`, defaultValue: `null`, importance: 'low', purpose: `The (optional) setting for the broker to use to verify that the JWT was created by the expected issuer. The JWT will be inspected for the standard OAuth…`, incomplete: true},
  {name: `sasl.oauthbearer.jwks.endpoint.refresh.ms`, type: `long`, defaultValue: `3600000 (1 hour)`, importance: 'low', purpose: `The (optional) value in milliseconds for the broker to wait between refreshing its JWKS (JSON Web Key Set) cache that contains the keys to verify the…`, incomplete: true},
  {name: `sasl.oauthbearer.jwks.endpoint.retry.backoff.max.ms`, type: `long`, defaultValue: `10000 (10 seconds)`, importance: 'low', purpose: `The (optional) value in milliseconds for the maximum wait between attempts to retrieve the JWKS (JSON Web Key Set) from the external authentication provider.`, incomplete: true},
  {name: `sasl.oauthbearer.jwks.endpoint.retry.backoff.ms`, type: `long`, defaultValue: `100`, importance: 'low', purpose: `The (optional) value in milliseconds for the initial wait between JWKS (JSON Web Key Set) retrieval attempts from the external authentication provider.…`, incomplete: true},
  {name: `sasl.oauthbearer.scope.claim.name`, type: `string`, defaultValue: `scope`, importance: 'low', purpose: `The OAuth claim for the scope is often named "scope", but this (optional) setting can provide a different name to use for the scope included in the JWT…`, incomplete: true},
  {name: `sasl.oauthbearer.sub.claim.name`, type: `string`, defaultValue: `sub`, importance: 'low', purpose: `The OAuth claim for the subject is often named "sub", but this (optional) setting can provide a different name to use for the subject included in the JWT…`, incomplete: true},
  {name: `security.providers`, type: `string`, defaultValue: `null`, importance: 'low', purpose: `A list of configurable creator classes each returning a provider implementing security algorithms. These classes should implement the \`org.apache.kafka.common.`, incomplete: true},
  {name: `ssl.allow.dn.changes`, type: `boolean`, defaultValue: `false`, importance: 'low', purpose: `Indicates whether changes to the certificate distinguished name should be allowed during a dynamic reconfiguration of certificates or not.`, incomplete: false},
  {name: `ssl.allow.san.changes`, type: `boolean`, defaultValue: `false`, importance: 'low', purpose: `Indicates whether changes to the certificate subject alternative names should be allowed during a dynamic reconfiguration of certificates or not.`, incomplete: false},
  {name: `ssl.endpoint.identification.algorithm`, type: `string`, defaultValue: `https`, importance: 'low', purpose: `The endpoint identification algorithm to validate server hostname using server certificate.`, incomplete: false},
  {name: `ssl.engine.factory.class`, type: `class`, defaultValue: `null`, importance: 'low', purpose: `The class of type org.apache.kafka.common.security.auth.SslEngineFactory to provide SSLEngine objects. Default value is org.apache.kafka.common.security.ssl.`, incomplete: true},
  {name: `ssl.principal.mapping.rules`, type: `string`, defaultValue: `DEFAULT`, importance: 'low', purpose: `A list of rules for mapping from distinguished name from the client certificate to short name. The rules are evaluated in order and the first rule that…`, incomplete: true},
  {name: `ssl.secure.random.implementation`, type: `string`, defaultValue: `null`, importance: 'low', purpose: `The SecureRandom PRNG implementation to use for SSL cryptography operations.`, incomplete: false},
  {name: `telemetry.max.bytes`, type: `int`, defaultValue: `1048576 (1 mebibyte)`, importance: 'low', purpose: `The maximum size (after compression if compression is used) of telemetry metrics pushed from a client to the broker. The default value is 1048576 (1 MB).`, incomplete: false},
  {name: `transaction.abort.timed.out.transaction.cleanup.interval.ms`, type: `int`, defaultValue: `10000 (10 seconds)`, importance: 'low', purpose: `The interval at which to rollback transactions that have timed out`, incomplete: false},
  {name: `transaction.partition.verification.enable`, type: `boolean`, defaultValue: `true`, importance: 'low', purpose: `Enable verification that checks that the partition has been added to the transaction before writing transactional records to the partition`, incomplete: false},
  {name: `transaction.remove.expired.transaction.cleanup.interval.ms`, type: `int`, defaultValue: `3600000 (1 hour)`, importance: 'low', purpose: `The interval at which to remove transactions that have expired due totransactional.id.expiration.ms passing`, incomplete: false},
];
