server:
  port: 8090

spring:
  application:
    name: spring-csrf-demo
  thymeleaf:
    cache: false

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    org.springframework.security: INFO
