server:
  port: 8093

spring:
  application:
    name: spring-web-attacks-demo
  datasource:
    url: jdbc:h2:mem:attacks;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql:
    init:
      mode: always

app:
  rate-limit:
    requests-per-window: 20
    window-seconds: 10
