server:
  port: 8092

spring:
  application:
    name: customer-mcp-server
  ai:
    mcp:
      server:
        name: customer-mcp-server
        version: 1.0.0
        protocol: STREAMABLE
        streamable-http:
          mcp-endpoint: /mcp

app:
  mcp:
    security:
      enabled: false
    api-key: dev-mcp-key

management:
  endpoints:
    web:
      exposure:
        include: health,info
