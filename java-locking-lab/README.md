# Java Locking Lab

JUnit 5 demos that back the `/java-locking` interview page.

```bash
cd java-locking-lab
mvn test
```

Covers synchronized invariants, atomics/LongAdder, ConcurrentHashMap `computeIfAbsent`, Semaphore/Latch/Barrier, and deadlock-prevention via ordered `tryLock`.
