# db-sharding-lab

Core Java shard routers for the Partitioning & Sharding interview hub.

```bash
mvn test
```

Includes: `HashShardRouter`, `ConsistentHashShardRouter`, `RangeShardRouter`, `DirectoryShardRouter`, `SnowflakeIdGenerator`.
