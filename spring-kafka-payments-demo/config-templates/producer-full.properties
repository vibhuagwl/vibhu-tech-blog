# Bootstrap + serialization
bootstrap.servers=localhost:9092
key.serializer=org.apache.kafka.common.serialization.StringSerializer
value.serializer=org.springframework.kafka.support.serializer.JsonSerializer

# Delivery safety
acks=all
enable.idempotence=true
retries=2147483647
max.in.flight.requests.per.connection=5
delivery.timeout.ms=120000
request.timeout.ms=30000
retry.backoff.ms=100
retry.backoff.max.ms=1000

# Throughput / batching / accumulator
linger.ms=25
batch.size=65536
buffer.memory=67108864
compression.type=zstd
max.request.size=1048576
send.buffer.bytes=131072

# Transaction / ordering / freshness
transactional.id=payment-api-tx-1
transaction.timeout.ms=60000
client.id=payment-api

# Metadata / reconnect
metadata.max.age.ms=300000
metadata.max.idle.ms=300000
reconnect.backoff.ms=50
reconnect.backoff.max.ms=1000

# DNS / connections
client.dns.lookup=use_all_dns_ips
connections.max.idle.ms=540000

# Security placeholders (prod)
security.protocol=SASL_SSL
sasl.mechanism=SCRAM-SHA-512
# sasl.jaas.config=org.apache.kafka.common.security.scram.ScramLoginModule required username="..." password="...";
# ssl.truststore.location=/etc/certs/truststore.jks
# ssl.truststore.password=changeit
# ssl.keystore.location=/etc/certs/keystore.jks
# ssl.keystore.password=changeit
# ssl.key.password=changeit
